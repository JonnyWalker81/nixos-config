---
phase: 07-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config.el
  - users/doom.d/config-org-visual.el
autonomous: true
must_haves:
  truths:
    - "Org buffers render with modern, clean hierarchy styling (headlines, keywords, tables, timestamps) instead of legacy ASCII-heavy visuals."
    - "Agenda buffers show modern styling after render, with compact readable section framing and clear but not heavy emphasis for high-priority sections."
  artifacts:
    - path: "users/doom.d/config-org-visual.el"
      provides: "Centralized visual configuration ownership for Org and agenda styling"
      contains: "use-package! org-modern, org-mode hook, org-agenda-finalize-hook, subtle visual variables"
    - path: "users/doom.d/config.el"
      provides: "Deterministic module load ordering for visual layer"
      contains: "(load! \"config-org-visual\") after org-gtd/org-agenda loaders"
  key_links:
    - from: "users/doom.d/config.el"
      to: "users/doom.d/config-org-visual.el"
      via: "Doom load chain"
      pattern: "load! \"config-org-visual\""
    - from: "users/doom.d/config-org-visual.el"
      to: "org buffers"
      via: "org-mode-hook"
      pattern: "org-mode-hook.*org-modern-mode"
    - from: "users/doom.d/config-org-visual.el"
      to: "agenda buffers"
      via: "org-agenda-finalize-hook"
      pattern: "org-agenda-finalize-hook.*org-modern-agenda"
---

<objective>
Establish a single visual owner module and deliver baseline org-modern styling for both Org and agenda buffers.

Purpose: Lock deterministic visual ownership and achieve VIS-01/VIS-02 foundations without changing workflow semantics.
Output: New `config-org-visual.el` module wired into bootstrap, with modern buffer + agenda styling defaults aligned to the phase context.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-visual-polish/07-CONTEXT.md
@.planning/phases/07-visual-polish/07-RESEARCH.md
@.planning/phases/03-basic-agenda/03-02-SUMMARY.md
@users/doom.d/config.el
@users/doom.d/config-org-agenda.el
@users/doom.d/config-org-gtd.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visual ownership module and deterministic loader placement</name>
  <files>users/doom.d/config-org-visual.el, users/doom.d/config.el</files>
  <action>Create `config-org-visual.el` as the sole owner for Org visual presentation and add `(load! "config-org-visual")` in `config.el` after existing org subsystem loaders (`config-org-gtd`, `config-org-agenda`, `config-org-roam`, `config-org-journal`, `config-org-denote`). Do not move behavior from semantic/workflow modules in this task; only establish load ownership and baseline scaffolding with `(provide 'config-org-visual)`.</action>
  <verify>`rg "config-org-visual|load! \"config-org-visual\"|provide 'config-org-visual" users/doom.d/config.el users/doom.d/config-org-visual.el` returns one loader entry and module provide.</verify>
  <done>Visual module exists, is loaded deterministically, and is positioned to safely override Doom/org defaults without touching GTD/agenda command semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Enable org-modern for Org buffers and agenda finalize pass</name>
  <files>users/doom.d/config-org-visual.el</files>
  <action>Configure `org-modern` in `config-org-visual.el` with `org-mode-hook` and `org-agenda-finalize-hook` so modern styling applies to both `.org` buffers and generated agenda buffers. Tune visual variables for the locked clean/minimal direction (subtle bullets/tables/timestamps, compact agenda readability, quiet group framing). Keep TODO/priority semantic face ownership untouched in this plan; defer color semantics and emphasis/preview behavior to 07-02.</action>
  <verify>`rg "use-package! org-modern|org-modern-mode|org-modern-agenda|org-agenda-finalize-hook" users/doom.d/config-org-visual.el` confirms both hooks and module configuration are present.</verify>
  <done>VIS-01 and VIS-02 foundations are implemented: org-modern runs in Org and agenda buffers with minimal, hierarchy-first styling defaults.</done>
</task>

</tasks>

<verification>
1. Run `~/.emacs.default/bin/doom sync` (or local Doom binary path in this repo) to ensure module/package resolution is intact.
2. Runtime check in an Org file: `(bound-and-true-p org-modern-mode)` is non-nil and visual hierarchy/timestamps are modernized.
3. Runtime check in `SPC o a d` and `SPC o a R`: agenda buffers show modernized rendering after finalize (not plain legacy output).
</verification>

<success_criteria>
Phase 7 visual baseline is in place: visual ownership is centralized and deterministic, and modern styling is active in both Org and agenda surfaces without altering command behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-polish/07-01-SUMMARY.md`
</output>
