---
phase: 07-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - users/doom.d/config-org-gtd.el
  - users/doom.d/config-org-visual.el
autonomous: false
must_haves:
  truths:
    - "TODO states use the locked color semantics in Org and agenda views: NEXT blue, WAITING orange, SOMEDAY grey, TODO red, DONE green, CANCELLED dim."
    - "Emphasis markers are hidden by default and only reveal near cursor/edit point with smooth, low-flicker behavior."
    - "Inline images and LaTeX fragments render automatically in Org buffers without manual toggle each session."
  artifacts:
    - path: "users/doom.d/config-org-gtd.el"
      provides: "Canonical TODO semantic color mapping aligned to roadmap constraints"
      contains: "org-todo-keyword-faces and subtle org-priority-faces tuned for visual layer compatibility"
    - path: "users/doom.d/config-org-visual.el"
      provides: "Emphasis reveal behavior and preview automation"
      contains: "use-package! org-appear, org-hide-emphasis-markers, inline image + LaTeX preview hooks"
  key_links:
    - from: "users/doom.d/config-org-gtd.el"
      to: "users/doom.d/config-org-agenda.el"
      via: "shared TODO face semantics used in agenda rendering"
      pattern: "org-todo-keyword-faces"
    - from: "users/doom.d/config-org-visual.el"
      to: "Org emphasis rendering"
      via: "org-appear mode and hide-marker settings"
      pattern: "org-appear-mode|org-hide-emphasis-markers|org-appear-delay"
    - from: "users/doom.d/config-org-visual.el"
      to: "Org buffer preview overlays"
      via: "org-mode hook invoking inline image and latex preview initialization"
      pattern: "org-display-inline-images|org-latex-preview|org-startup-with-inline-images|org-startup-with-latex-preview"
---

<objective>
Complete Phase 7 semantic visual behavior: locked TODO colors, smooth emphasis reveal, and automatic inline image/LaTeX previews, then validate all VIS requirements.

Purpose: Deliver the user-visible polish outcomes that make Org editing and review visually clear while preserving GTD meaning.
Output: Updated semantic face definitions and visual interaction behaviors with requirement-indexed verification evidence.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-visual-polish/07-CONTEXT.md
@.planning/phases/07-visual-polish/07-RESEARCH.md
@.planning/phases/07-visual-polish/07-01-SUMMARY.md
@.planning/phases/01-gtd-foundation/01-02-SUMMARY.md
@.planning/phases/03-basic-agenda/03-02-SUMMARY.md
@users/doom.d/config-org-gtd.el
@users/doom.d/config-org-visual.el
@users/doom.d/config-org-agenda.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock TODO and priority visual semantics to roadmap colors</name>
  <files>users/doom.d/config-org-gtd.el, users/doom.d/config-org-visual.el</files>
  <action>Ensure `org-todo-keyword-faces` exactly matches VIS-03 semantics (NEXT blue, WAITING orange, SOMEDAY grey, TODO red, DONE green, CANCELLED dim) with moderate saturation and NEXT as the strongest open-state signal. Keep priority as a subtle overlay in `org-priority-faces`, and prevent org-modern from replacing semantic TODO/priority meaning with heavy badges (use org-modern face knobs rather than custom overlays).</action>
  <verify>`rg "org-todo-keyword-faces|NEXT|WAITING|SOMEDAY|TODO|DONE|CANCELLED|org-priority-faces|org-modern-.*faces" users/doom.d/config-org-gtd.el users/doom.d/config-org-visual.el` shows explicit semantic mapping and subtle priority behavior.</verify>
  <done>VIS-03 is achieved with exact state color semantics and priority remaining secondary to TODO state meaning.</done>
</task>

<task type="auto">
  <name>Task 2: Add smooth markup reveal and automatic inline preview behaviors</name>
  <files>users/doom.d/config-org-visual.el</files>
  <action>Configure `org-appear` in the visual module with reveal-at-point behavior and short anti-flicker delay so emphasis markers reappear only where editing focus is active. Enable `org-hide-emphasis-markers` globally for Org buffers. Add startup/hook behavior to auto-display inline images and auto-render LaTeX fragments when Org buffers open, while avoiding expensive refresh loops on every keystroke.</action>
  <verify>`rg "use-package! org-appear|org-hide-emphasis-markers|org-appear-delay|org-display-inline-images|org-startup-with-inline-images|org-startup-with-latex-preview|org-latex-preview" users/doom.d/config-org-visual.el` confirms reveal and preview automation paths.</verify>
  <done>VIS-04 and VIS-05 are implemented: markers are hidden with contextual reveal, and inline images/LaTeX previews appear automatically in Org buffers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 7 visual polish behavior across Org and agenda (VIS-01 through VIS-05).</what-built>
  <how-to-verify>
    1. Run `~/.emacs.default/bin/doom sync` and restart Emacs.
    2. Open `~/org/gtd/inbox.org`; confirm modern headline/table/timestamp styling and clean hierarchy.
    3. In that buffer, type or navigate over `*bold*` and `/italic/`; markers should reveal at point and hide smoothly when point leaves.
    4. Add an image link (`[[file:...png]]`) and a LaTeX fragment (`$E=mc^2$`), reopen/refresh the buffer, and confirm inline rendering appears automatically.
    5. Open `SPC o a d` and `SPC o a R`; verify modern agenda styling is applied and TODO state colors follow locked semantics.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the exact mismatch (buffer, command, and observed behavior).</resume-signal>
</task>

</tasks>

<verification>
1. Static check: `rg "config-org-visual|org-appear|org-modern|org-startup-with-inline-images|org-startup-with-latex-preview" users/doom.d/*.el` confirms ownership and wiring.
2. Runtime check: Doom sync completes and no Org visual package errors appear in `*Messages*`.
3. Human checkpoint confirms VIS-01..VIS-05 with direct interaction evidence.
</verification>

<success_criteria>
All Phase 7 visual requirements are verifiably true: modern Org/agenda styling is active, TODO colors encode exact roadmap semantics, markup reveal is smooth and contextual, and inline image/LaTeX previews auto-render in Org buffers.
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-polish/07-02-SUMMARY.md`
</output>
