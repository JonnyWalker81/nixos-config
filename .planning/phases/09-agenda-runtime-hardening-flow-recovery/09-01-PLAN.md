---
phase: 09-agenda-runtime-hardening-flow-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config-org-agenda.el
  - users/doom.d/config-org-integration.el
autonomous: true
must_haves:
  truths:
    - "Daily (`d`) and weekly/review (`w`/`r`/`R`) agenda commands execute without `void-variable` runtime failures."
    - "org-super-agenda grouping remains available at command execution time, with safe fallback behavior when not yet bound."
    - "Dashboard quick actions and canonical OrgLife agenda wrappers use the same hardened runtime path."
  artifacts:
    - path: "users/doom.d/config-org-agenda.el"
      provides: "runtime-safe org-super-agenda resolver and hardened agenda dispatch wrappers"
      contains: "org-life-agenda"
    - path: "users/doom.d/config-org-integration.el"
      provides: "dashboard/canonical agenda actions routed through hardened agenda wrappers"
      contains: "org-life-dashboard-action"
  key_links:
    - from: "users/doom.d/config-org-agenda.el"
      to: "org-agenda-custom-commands"
      via: "command options resolve groups through guarded helper rather than raw dereference"
      pattern: "org-super-agenda-groups"
    - from: "users/doom.d/config-org-integration.el"
      to: "users/doom.d/config-org-agenda.el"
      via: "dashboard and `SPC o` agenda commands call hardened wrappers before agenda dispatch"
      pattern: "org-life-agenda"
---

<objective>
Harden agenda runtime wiring so AGN-01/02/03 command paths no longer fail when `org-super-agenda-groups` is unbound at execution time.

Purpose: Phase 9 exists to remove the cross-phase runtime blocker that breaks Phase 3 requirements and downstream review/dashboard flows.
Output: Runtime-safe agenda helpers in agenda module plus integration/dashboard command routing onto those hardened helpers.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/09-agenda-runtime-hardening-flow-recovery/09-RESEARCH.md
@.planning/phases/03-basic-agenda/03-02-SUMMARY.md
@.planning/phases/04-advanced-agenda-gtd-reviews/04-02-SUMMARY.md
@.planning/phases/08-integration-dashboard/08-03-SUMMARY.md
@users/doom.d/config-org-agenda.el
@users/doom.d/config-org-integration.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime-safe super-agenda preparation and group resolution</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Create one canonical default group definition variable and a guarded resolver/helper that never throws when `org-super-agenda-groups` is unbound. Add a pre-dispatch runtime preparation function that best-effort loads `org-super-agenda`, enables mode when available, and binds fallback groups when missing. Replace all command-level uses of raw `(org-super-agenda-groups org-super-agenda-groups)` with the safe resolver output so block and command settings do not dereference an unbound symbol.</action>
  <verify>`emacs --batch -Q --eval "(progn (load-file \"users/doom.d/config-org-agenda.el\") (princ \"agenda-load-ok\"))"` completes and `rg "org-life-agenda.*(prepare|groups|safe)" users/doom.d/config-org-agenda.el` shows the runtime helpers and safe group wiring.</verify>
  <done>Agenda command definitions no longer rely on unsafe direct variable dereference and have explicit runtime hardening helpers in place.</done>
</task>

<task type="auto">
  <name>Task 2: Route all agenda key entrypoints through hardened dispatch wrappers</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Introduce canonical wrapper functions for agenda opens (`d`, `w`, `r`, `R`, `I`, `H`, `W`) that call runtime preparation then dispatch `org-agenda`. Update agenda keybindings in this module to call wrappers instead of direct `(org-agenda nil KEY)`, preserving existing keys and command names. Keep behavior/output contracts unchanged except crash prevention.</action>
  <verify>`emacs --batch -Q --eval "(progn (load-file \"users/doom.d/config-org-agenda.el\") (mapc (lambda (fn) (unless (fboundp fn) (error \"missing %s\" fn))) '(org-life-agenda-daily-planning org-life-agenda-weekly-planning org-life-agenda-daily-review org-life-agenda-weekly-review org-life-agenda-inbox-dashboard org-life-agenda-context-home org-life-agenda-context-work)) (princ \"agenda-wrappers-ok\"))"` succeeds.</verify>
  <done>Every agenda/review/context command path in the agenda module now executes through one hardened runtime wrapper layer.</done>
</task>

<task type="auto">
  <name>Task 3: Harden dashboard quick actions and integration wrappers to reuse agenda safety path</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Update dashboard quick action handlers and integration-level agenda wrapper functions to call agenda-module hardened wrappers instead of raw `org-agenda` calls. Ensure this covers daily review, weekly review, and all canonical agenda wrappers used by `SPC o` contracts so Phase 8 integrations inherit runtime safety automatically.</action>
  <verify>`emacs --batch -Q --eval "(progn (load-file \"users/doom.d/config-org-agenda.el\") (load-file \"users/doom.d/config-org-integration.el\") (princ \"integration-load-ok\"))"` succeeds and `rg "org-life-dashboard-action-(daily|weekly)-review|org-life-agenda-(daily|weekly|inbox|context)" users/doom.d/config-org-integration.el` confirms wrapper-based dispatch.</verify>
  <done>Dashboard and integration agenda entrypoints share the hardened runtime path, removing split behavior between manual agenda use and dashboard quick actions.</done>
</task>

</tasks>

<verification>
Run `tests/run-orglife-tests.sh` after implementation and ensure no existing OrgLife regressions before proceeding to Phase 9 regression additions.
</verification>

<success_criteria>
- Runtime helper layer exists and is used by all AGN-01/02/03 command definitions.
- `d`/`w`/`r`/`R` plus dashboard-linked agenda opens are routed through hardened wrappers.
- No direct unsafe `org-super-agenda-groups` self-dereference remains in agenda command option wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/09-agenda-runtime-hardening-flow-recovery/09-01-SUMMARY.md`
</output>
