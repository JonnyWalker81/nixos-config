---
phase: 09-agenda-runtime-hardening-flow-recovery
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tests/emacs/orglife-config-tests.el
  - .planning/phases/09-agenda-runtime-hardening-flow-recovery/09-agenda-runtime-hardening-flow-recovery-VERIFICATION.md
autonomous: true
must_haves:
  truths:
    - "Regression coverage reproduces the historical unbound/load-timing failure mode and proves agenda paths survive it."
    - "Agenda-dependent flows (capture->agenda review, project/meeting->weekly review, journal->agenda visibility, dashboard agenda quick actions) are verified as restored."
    - "Phase 9 verification artifact records AGN-01/02/03 closure with executable evidence."
  artifacts:
    - path: "tests/emacs/orglife-config-tests.el"
      provides: "execution-path regression tests for agenda runtime hardening and dashboard flow recovery"
      contains: "orglife"
    - path: ".planning/phases/09-agenda-runtime-hardening-flow-recovery/09-agenda-runtime-hardening-flow-recovery-VERIFICATION.md"
      provides: "phase verification mapping AGN-01/02/03 and E2E flow recovery evidence"
      contains: "status"
  key_links:
    - from: "tests/emacs/orglife-config-tests.el"
      to: "users/doom.d/config-org-agenda.el"
      via: "tests unbind or defer super-agenda state and execute hardened wrappers"
      pattern: "makunbound|org-life-agenda"
    - from: "tests/emacs/orglife-config-tests.el"
      to: "users/doom.d/config-org-integration.el"
      via: "dashboard quick actions invoke agenda wrappers without runtime crash"
      pattern: "dashboard-action|weekly-review|daily-review"
---

<objective>
Close Phase 9 by adding anti-regression coverage for agenda runtime safety and writing the verification artifact that proves AGN-01/02/03 and flow recovery outcomes.

Purpose: A runtime fix without targeted regression simulation can silently regress; this plan locks in the fix and records auditable closure evidence.
Output: New ERT tests for unbound/load-timing scenarios, full OrgLife suite pass, and phase verification document.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/09-agenda-runtime-hardening-flow-recovery/09-RESEARCH.md
@.planning/phases/09-agenda-runtime-hardening-flow-recovery/09-01-SUMMARY.md
@tests/emacs/orglife-config-tests.el
@tests/run-orglife-tests.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unbound/load-timing regression tests for agenda command execution paths</name>
  <files>tests/emacs/orglife-config-tests.el</files>
  <action>Add focused ERT coverage that intentionally reproduces the historical failure mode by unbinding or withholding `org-super-agenda-groups` at invocation time, then executing hardened agenda wrappers for `d`/`w`/`r`/`R` and asserting no `void-variable` crash. Ensure tests model timing sensitivity instead of only asserting static config presence, and avoid stubs that hide execution-path failures.</action>
  <verify>`tests/run-orglife-tests.sh` passes and includes new tests that would fail if `org-super-agenda-groups` is dereferenced unsafely during command execution.</verify>
  <done>Regression suite catches the original runtime class and proves hardened agenda wrappers survive unbound/load-timing conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration regression tests for dashboard and restored agenda-dependent flows</name>
  <files>tests/emacs/orglife-config-tests.el</files>
  <action>Extend ERT to verify dashboard agenda quick actions (`Daily Review`, `Weekly Review`) and integration wrappers dispatch through hardened agenda paths, plus add representative flow-level assertions for capture->agenda review, project/meeting->weekly review, and journal->agenda visibility legs using fixture files and wrapper invocation checks. Keep tests deterministic and batch-safe.</action>
  <verify>`tests/run-orglife-tests.sh` passes with new flow-recovery tests and no regressions in existing OrgLife coverage.</verify>
  <done>Automated tests confirm broken audit flows are executable again through hardened runtime paths, including dashboard quick actions.</done>
</task>

<task type="auto">
  <name>Task 3: Produce Phase 9 verification artifact with AGN and flow-closure evidence</name>
  <files>.planning/phases/09-agenda-runtime-hardening-flow-recovery/09-agenda-runtime-hardening-flow-recovery-VERIFICATION.md</files>
  <action>Create/update the phase verification document with requirement-indexed evidence for AGN-01, AGN-02, and AGN-03, plus explicit integration/flow closure checks from the milestone audit (`capture->agenda`, `project/meeting->weekly review`, `journal->agenda`, dashboard quick-action agenda opens). Include commands/tests run, outcomes, and pass/fail status in the same style as prior phase verification artifacts.</action>
  <verify>Verification artifact exists, references AGN-01/02/03 explicitly, and documents successful run output from `tests/run-orglife-tests.sh` plus any targeted batch validation commands used for flow checks.</verify>
  <done>Phase 9 has auditable proof that agenda runtime failures are closed and downstream integrations/flows are restored end-to-end.</done>
</task>

</tasks>

<verification>
Execute `tests/run-orglife-tests.sh` as mandatory gate, then confirm verification artifact marks AGN-01/02/03 and all four phase success truths as passed with concrete evidence.
</verification>

<success_criteria>
- New regression tests explicitly simulate unbound/load-timing conditions and pass on hardened code.
- Dashboard and integration agenda paths are covered by automated tests, not only manual assumptions.
- `.planning/phases/09-agenda-runtime-hardening-flow-recovery/09-agenda-runtime-hardening-flow-recovery-VERIFICATION.md` records requirement and flow closure with executable evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/09-agenda-runtime-hardening-flow-recovery/09-02-SUMMARY.md`
</output>
