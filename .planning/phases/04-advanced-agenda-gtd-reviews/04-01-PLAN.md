---
phase: 04-advanced-agenda-gtd-reviews
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config-org-agenda.el
autonomous: true
must_haves:
  truths:
    - "User can open a dedicated Daily Review agenda that combines today's timeline, Priority A actions, NEXT actions, WAITING items, and an explicit inbox count/triage section in one flow."
    - "User can open context-only review agendas for @home and @work with single-key entrypoints."
    - "Daily review views remain planning-focused (open states only by default) and keep existing Phase 3 daily/weekly commands intact."
  artifacts:
    - path: "users/doom.d/config-org-agenda.el"
      provides: "New Daily Review and context-filter command definitions"
      contains: "org-agenda-custom-commands entries for daily review + @home/@work filters"
    - path: "users/doom.d/config-org-agenda.el"
      provides: "Review keybinding entrypoints under Doom leader"
      contains: "map! bindings for new review commands"
  key_links:
    - from: "users/doom.d/config-org-agenda.el"
      to: "org-agenda custom command dispatcher"
      via: "new command keys wired into org-agenda-custom-commands"
      pattern: "\(\".*Daily Review\|@home\|@work\""
    - from: "users/doom.d/config-org-agenda.el"
      to: "SPC o A keymap"
      via: "leader bindings call (org-agenda nil \"<key>\") for each review view"
      pattern: "map!.*org-agenda nil"
---

<objective>
Add the advanced daily review and context-filtered review entrypoints required for Phase 4.

Purpose: Deliver AGN-04 and AGN-06 foundations by giving the user focused daily triage and one-keystroke context filtering without disrupting Phase 3 planning flows.
Output: Extended `org-agenda-custom-commands` and keybindings in `config-org-agenda.el` for Daily Review + @home/@work review views.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-basic-agenda/03-01-SUMMARY.md
@.planning/phases/03-basic-agenda/03-02-SUMMARY.md
@users/doom.d/config-org-gtd.el
@users/doom.d/config-org-agenda.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a purpose-built Daily Review command</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Extend `org-agenda-custom-commands` with a new Daily Review command that keeps timeline-first flow and adds explicit review blocks for: high-priority actionable work (Priority A TODO/NEXT), all NEXT actions, WAITING items, and inbox triage entries. Include a dedicated inbox block/header that surfaces inbox item count directly in the view (for example via block title/count display behavior or explicit count expression captured in header text). Keep collection in agenda/tags-todo blocks and preserve Phase 3 grouping behavior by reusing existing super-agenda patterns. Do not remove or rename existing `d`/`w` planning commands.</action>
  <verify>Run `rg "Daily Review|Priority A|NEXT|WAITING|inbox|count|org-agenda-custom-commands|\(\"d\" \"Daily planning\"|\(\"w\" \"Weekly planning\"" users/doom.d/config-org-agenda.el` and confirm new Daily Review exists with explicit inbox count intent while existing `d`/`w` commands remain present.</verify>
  <done>Daily Review command exists and includes all AGN-04 sections, including an explicit inbox count/triage section in one command flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add single-key context-filtered review commands for @home and @work</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Add dedicated context-filter custom agenda commands that show only actionable tasks tagged `@home` and `@work` respectively. Ensure filters include TODO/NEXT states, keep WAITING optional but visible if tagged to the context, and avoid cross-context leakage. Add Doom leader bindings under the existing agenda prefix so each context view is reachable with one additional key from `SPC o A`.</action>
  <verify>Run `rg "@home|@work|map!|SPC o A|org-agenda nil" users/doom.d/config-org-agenda.el` and confirm both context commands and leader keybindings are wired.</verify>
  <done>User can open @home-only and @work-only review views from single-key agenda entrypoints.</done>
</task>

<task type="auto">
  <name>Task 3: Stabilize review metadata and command descriptions for discoverability</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Ensure new review commands use clear dispatcher names, planning-oriented headers, and metadata-rich prefix formatting consistent with Phase 3 defaults. Keep DONE/CANCELLED hidden in review blocks by default. Reuse existing prefix format style unless a block-specific override is required for readability.</action>
  <verify>Run `rg "org-agenda-prefix-format|DONE|CANCELLED|Daily Review|@home|@work|org-agenda-overriding-header" users/doom.d/config-org-agenda.el` and confirm review commands are labeled clearly with open-state-focused defaults.</verify>
  <done>Daily/context review commands are discoverable, readable, and consistent with existing planning metadata conventions.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-agenda.el\") (check-parens))"` exits successfully.
2. Static checks confirm Daily Review plus @home/@work commands and keybindings exist without removing `d`/`w`, and Daily Review includes explicit inbox count intent.
3. `~/.emacs.d/bin/doom sync` succeeds after command additions.
</verification>

<success_criteria>
Phase 4 review entrypoints are established: user has a dedicated Daily Review command and one-keystroke context-filtered agendas for @home and @work, with stable metadata and no regression to Phase 3 daily/weekly planning commands.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-agenda-gtd-reviews/04-01-SUMMARY.md`
</output>
