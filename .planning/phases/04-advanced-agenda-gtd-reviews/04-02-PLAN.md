---
phase: 04-advanced-agenda-gtd-reviews
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - users/doom.d/config-org-agenda.el
autonomous: true
must_haves:
  truths:
    - "User can open a dedicated Weekly Review agenda that includes week timeline, stuck projects, WAITING, SOMEDAY, and unprocessed inbox sections."
    - "Stuck projects (projects without NEXT actions) are explicitly visible in weekly review or a dedicated linked view."
    - "Weekly review keeps GTD decision flow intact: triage inbox, inspect stalled commitments, then review parked items."
  artifacts:
    - path: "users/doom.d/config-org-agenda.el"
      provides: "Weekly Review command and stuck-project detection wiring"
      contains: "org-agenda-custom-commands weekly review entry with stuck projects section"
    - path: "users/doom.d/config-org-agenda.el"
      provides: "Validation-ready keybinding access for weekly review"
      contains: "map! binding for weekly review command"
  key_links:
    - from: "users/doom.d/config-org-agenda.el"
      to: "project task structure in ~/org/gtd/projects.org"
      via: "stuck-project query that flags projects missing NEXT children"
      pattern: "stuck|org-stuck-projects|projects"
    - from: "users/doom.d/config-org-agenda.el"
      to: "weekly review agenda command"
      via: "block sequence includes timeline + stuck + waiting + someday + inbox"
      pattern: "Weekly Review|WAITING|SOMEDAY|inbox"
    - from: "users/doom.d/config-org-agenda.el"
      to: "SPC o A keymap"
      via: "leader binding calls (org-agenda nil \"<weekly-review-key>\")"
      pattern: "map!.*org-agenda nil"
---

<objective>
Complete advanced weekly review workflow with stuck project detection and end-to-end validation.

Purpose: Deliver AGN-05 and AGN-07 by ensuring the user can reliably find stalled projects and run a complete weekly GTD review from one command.
Output: Weekly Review agenda command, stuck-project detection logic, and verification evidence for all Phase 4 success criteria.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-advanced-agenda-gtd-reviews/04-01-SUMMARY.md
@.planning/phases/03-basic-agenda/03-02-SUMMARY.md
@users/doom.d/config-org-gtd.el
@users/doom.d/config-org-agenda.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Weekly Review block agenda with GTD review sections</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Add a Weekly Review command (separate from existing `w` weekly planning command) that includes: week timeline, dedicated stuck-project section, all WAITING items, SOMEDAY/MAYBE parking section, and unprocessed inbox section. Keep Monday week start and preserve existing weekly planning command behavior. Add a dedicated Doom leader binding for Weekly Review under the existing `SPC o A` prefix so the new review command is directly accessible without replacing prior planning bindings. Use clear section headers that map directly to weekly GTD review steps.</action>
  <verify>Run `rg "Weekly Review|Week timeline|stuck|WAITING|SOMEDAY|inbox|org-agenda-start-on-weekday 1|map!|org-agenda nil|\(\"w\" \"Weekly planning\"" users/doom.d/config-org-agenda.el` and confirm new Weekly Review exists, includes key sections, and has a leader binding while existing planning weekly command remains.</verify>
  <done>Weekly Review command exists with all required AGN-05 sections, a dedicated leader keybinding, and no regression to prior weekly planning command.</done>
</task>

<task type="auto">
  <name>Task 2: Wire robust stuck-project detection for GTD project files</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Implement stuck-project detection using Org-native stuck project capabilities (`stuck` agenda block and/or `org-stuck-projects`) tuned for this repo's TODO workflow (project has TODO/NEXT descendants; stuck means no NEXT). Scope detection to GTD project files so the output reflects actionable project commitments, not unrelated notes. Keep detection logic explicit in config comments or variable names so future phases can reuse it.</action>
  <verify>Run `rg "org-stuck-projects|stuck|NEXT|projects.org|org-agenda-custom-commands" users/doom.d/config-org-agenda.el` and confirm explicit stuck-project criteria and scope are present.</verify>
  <done>Weekly review can surface projects missing NEXT actions with criteria aligned to existing GTD states.</done>
</task>

<task type="auto">
  <name>Task 3: Execute validation gate for AGN-04 through AGN-07</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Run `~/.emacs.d/bin/doom sync`, then validate agenda commands in runtime (`M-x org-agenda` command keys for Daily Review, Weekly Review, and context filters). Confirm: Daily Review has required sections, Weekly Review has required sections including stuck projects and inbox triage, @home/@work filters isolate context tasks, and at least one seeded stuck project appears in stuck output until a NEXT child is added. Capture evidence in summary with exact command keys and observed section names.</action>
  <verify>Record validation evidence in summary with: command keys used, section headers observed, one stuck-project reproduction (before/after adding NEXT), and confirmation that @home and @work views exclude opposite context items.</verify>
  <done>All Phase 4 success criteria are proven with repeatable validation evidence and no command regressions from Phase 3.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-agenda.el\") (check-parens))"` exits successfully.
2. `~/.emacs.d/bin/doom sync` completes.
3. Runtime validation confirms AGN-04, AGN-05, AGN-06, and AGN-07 behavior with captured evidence in summary.
</verification>

<success_criteria>
User can complete a full weekly GTD review from dedicated agenda commands: daily and weekly review flows exist, context filters are single-key accessible, and stuck projects are clearly detectable and verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-agenda-gtd-reviews/04-02-SUMMARY.md`
</output>
