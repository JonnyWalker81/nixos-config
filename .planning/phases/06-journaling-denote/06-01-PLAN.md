---
phase: 06-journaling-denote
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config.el
  - users/doom.d/config-org-journal.el
autonomous: true
must_haves:
  truths:
    - "User can open today's journal from a dedicated keybinding and land in today's per-day file."
    - "Today's journal entry uses a lightweight daily scaffold with timestamped entries, a tasks section, and an end-of-day reflection prompt."
    - "Opening a new day carries over unfinished tasks from yesterday and marks yesterday's source items as migrated without deleting them."
  artifacts:
    - path: "users/doom.d/config-org-journal.el"
      provides: "org-journal daily configuration, file header shape, carry-over logic, and journal entry commands"
      contains: "org-journal-dir, org-journal-file-type, org-journal-carryover-items, org-journal-handle-old-carryover-fn"
    - path: "users/doom.d/config.el"
      provides: "deterministic loading of the journal module"
      contains: "(load! \"config-org-journal\")"
  key_links:
    - from: "users/doom.d/config.el"
      to: "users/doom.d/config-org-journal.el"
      via: "Doom config loader chain"
      pattern: "load! \"config-org-journal\""
    - from: "users/doom.d/config-org-journal.el"
      to: "~/org/journal/"
      via: "org-journal daily file settings"
      pattern: "org-journal-dir|org-journal-file-type"
    - from: "users/doom.d/config-org-journal.el"
      to: "yesterday journal tasks"
      via: "carry-over matcher and old-item handler"
      pattern: "org-journal-carryover-items|org-journal-handle-old-carryover-fn"
---

<objective>
Establish org-journal as a first-class module with daily journal files, timestamped entry structure, and automatic yesterday-only carry-over behavior with migration traceability.

Purpose: Deliver the core journaling engine so daily writing and task continuity work before agenda/capture/denote integration.
Output: A new `config-org-journal.el` module wired into Doom bootstrap, with daily file scaffolding and carry-over marker behavior.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-journaling-denote/06-CONTEXT.md
@.planning/phases/06-journaling-denote/06-RESEARCH.md
@.planning/phases/01-gtd-foundation/01-01-SUMMARY.md
@users/doom.d/config.el
@users/doom.d/config-org-gtd.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create org-journal owner module and wire deterministic loading</name>
  <files>users/doom.d/config-org-journal.el, users/doom.d/config.el</files>
  <action>Create `config-org-journal.el` as the single owner for journal behavior and add `(load! "config-org-journal")` in `config.el` immediately after existing Org subsystem loaders. Include idempotent directory bootstrapping for `~/org/journal/` in the journal module so daily file creation cannot fail on first run.</action>
  <verify>`rg "config-org-journal" users/doom.d/config.el` shows one loader entry, and `users/doom.d/config-org-journal.el` exists with `(provide 'config-org-journal)`.</verify>
  <done>Journal module exists, is loaded by Doom bootstrap, and ensures `~/org/journal/` is present.</done>
</task>

<task type="auto">
  <name>Task 2: Configure daily journal format and open-today entrypoint</name>
  <files>users/doom.d/config-org-journal.el</files>
  <action>In an `after! org` block, configure org-journal daily mode (`org-journal-dir`, daily file behavior, timestamp format/prefix, lightweight `org-journal-file-header`) that includes a dedicated tasks section and an end-of-day reflection prompt. Add an explicit keybinding entrypoint to open today's journal file/new entry (under an Org prefix) so JRN-01 is reachable in two keystrokes from the Org prefix map.</action>
  <verify>`rg "org-journal-dir|org-journal-file-type|org-journal-file-header|org-journal-time-format|map!" users/doom.d/config-org-journal.el` returns all expected settings and command binding.</verify>
  <done>Opening today's journal is bound and daily files default to the locked lightweight structure with timestamps, tasks, and reflection prompt.</done>
</task>

<task type="auto">
  <name>Task 3: Implement yesterday-only carry-over with migration marking</name>
  <files>users/doom.d/config-org-journal.el</files>
  <action>Implement carry-over for all open TODO states using `org-journal-carryover-items`, wire automatic carry-over when creating/opening today's entry, and set `org-journal-handle-old-carryover-fn` to a custom function that preserves source tasks and marks them as migrated (without deleting original entries). Restrict behavior to yesterday as the carry-over source to match locked decisions.</action>
  <verify>`rg "org-journal-carryover-items|org-journal-handle-old-carryover-fn|migrated|org-journal-new-entry" users/doom.d/config-org-journal.el` confirms matcher, handler, and automatic trigger path.</verify>
  <done>Unfinished journal tasks carry from yesterday to today automatically, and source tasks remain in the previous day marked as migrated.</done>
</task>

</tasks>

<verification>
1. Run `~/.emacs.default/bin/doom sync` successfully after adding the new module.
2. In batch/runtime checks, confirm journal module loads and key journal variables/functions are bound.
3. Validate by opening two consecutive day entries in a test journal directory: open tasks from yesterday appear in today while prior-day items remain tagged/marked migrated.
</verification>

<success_criteria>
JRN-01 and JRN-02 foundations are implemented: daily journal files and timestamped entry shape are active, a direct open-today keypath exists, and carry-over behavior preserves migration traceability from yesterday to today.
</success_criteria>

<output>
After completion, create `.planning/phases/06-journaling-denote/06-01-SUMMARY.md`
</output>
