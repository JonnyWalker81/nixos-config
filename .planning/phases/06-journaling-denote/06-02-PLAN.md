---
phase: 06-journaling-denote
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - users/doom.d/config.el
  - users/doom.d/config-org-agenda.el
  - users/doom.d/config-org-denote.el
  - users/doom.d/config-org-gtd.el
  - users/doom.d/config-org-journal.el
autonomous: true
must_haves:
  truths:
    - "Journal TODOs appear in agenda views in a dedicated journal section alongside, but not mixed into, GTD sections."
    - "User can capture a journal entry through org-capture into today's org-journal location without duplicate heading artifacts."
    - "User can run journal search across full journal history by default."
    - "Denote creates structured notes in ~/org/denote/ with strict keyword vocabulary, separate from org-roam."
  artifacts:
    - path: "users/doom.d/config-org-gtd.el"
      provides: "CAP-05 journal capture template integrated with existing canonical capture template set"
      contains: "journal capture key/template using org-journal location helper"
    - path: "users/doom.d/config-org-agenda.el"
      provides: "journal-specific agenda blocks/sections"
      contains: "journal section headers and journal-scoped org-agenda-files usage"
    - path: "users/doom.d/config-org-denote.el"
      provides: "strict denote directory and taxonomy configuration"
      contains: "denote-directory, denote-known-keywords, denote-infer-keywords"
    - path: "users/doom.d/config.el"
      provides: "deterministic loading for denote module"
      contains: "(load! \"config-org-denote\")"
  key_links:
    - from: "users/doom.d/config-org-gtd.el"
      to: "users/doom.d/config-org-journal.el"
      via: "org-capture template target function"
      pattern: "org-capture-templates|org-journal-new-entry"
    - from: "users/doom.d/config-org-agenda.el"
      to: "~/org/journal/"
      via: "journal-specific agenda section scope"
      pattern: "org-agenda-files.*journal|Journal"
    - from: "users/doom.d/config-org-denote.el"
      to: "~/org/denote/"
      via: "denote directory + strict keyword controls"
      pattern: "denote-directory|denote-known-keywords|denote-infer-keywords"
---

<objective>
Integrate journal workflows into capture, agenda, and search, then configure denote as a separate strict-taxonomy note system.

Purpose: Complete the user-facing journaling + denote outcomes so entries are captured/searchable/plannable and structured note naming is operationally separated from org-roam.
Output: CAP-05 journal capture template, journal agenda/search integration, and a dedicated `config-org-denote.el` module wired into Doom.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-journaling-denote/06-CONTEXT.md
@.planning/phases/06-journaling-denote/06-RESEARCH.md
@.planning/phases/02-capture-workflow/02-01-SUMMARY.md
@.planning/phases/03-basic-agenda/03-01-SUMMARY.md
@.planning/phases/05-knowledge-base/05-01-SUMMARY.md
@users/doom.d/config-org-agenda.el
@users/doom.d/config-org-gtd.el
@users/doom.d/config-org-journal.el
@users/doom.d/config-org-roam.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add journal agenda visibility and full-history search defaults</name>
  <files>users/doom.d/config-org-journal.el, users/doom.d/config-org-agenda.el</files>
  <action>Update journal configuration to ensure `~/org/journal/` is explicitly included in agenda scope (do not rely solely on built-in org-journal agenda integration). Update agenda command definitions so journal TODOs appear in a dedicated journal section in daily/weekly/review views, separate from GTD blocks. Add a journal search command/binding that defaults to searching full journal history.</action>
  <verify>`rg "~/org/journal/|Journal|org-journal-search|org-agenda-custom-commands" users/doom.d/config-org-journal.el users/doom.d/config-org-agenda.el` confirms explicit agenda scope, section headers, and full-history search path.</verify>
  <done>JRN-03 and JRN-04 behavior is wired: journal tasks are agenda-visible in their own section and journal search targets full history.</done>
</task>

<task type="auto">
  <name>Task 2: Extend canonical capture templates with CAP-05 journal capture</name>
  <files>users/doom.d/config-org-gtd.el, users/doom.d/config-org-journal.el</files>
  <action>Extend the existing single `org-capture-templates` set in `config-org-gtd.el` with a journal template key that uses an org-journal location helper from `config-org-journal.el` calling `(org-journal-new-entry t)` to avoid duplicate headings. Preserve the locked existing `t/i/p/m` templates unchanged and add CAP-05 as an additional entry in the same canonical set.</action>
  <verify>`rg "org-capture-templates|Journal|org-journal-new-entry t|function" users/doom.d/config-org-gtd.el users/doom.d/config-org-journal.el` shows the integrated template and helper function.</verify>
  <done>CAP-05 is implemented: `org-capture` can create journal entries in today's journal location without extra duplicate heading artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Create dedicated denote module with strict taxonomy and separation</name>
  <files>users/doom.d/config-org-denote.el, users/doom.d/config.el</files>
  <action>Create `config-org-denote.el` as sole denote owner: ensure `~/org/denote/` exists, set `denote-directory` to that path, define a strict initial `denote-known-keywords` vocabulary for reference/project-support use, set `denote-infer-keywords` to nil, and add denote-specific keybindings under an Org note prefix. Wire deterministic loading via `config.el`, keeping denote operationally separate from org-roam commands and directories.</action>
  <verify>`rg "config-org-denote|denote-directory|denote-known-keywords|denote-infer-keywords|map!" users/doom.d/config.el users/doom.d/config-org-denote.el` confirms load wiring and strict configuration.</verify>
  <done>KB-07 is implemented with strict naming in `~/org/denote/` and clear separation from org-roam workflow ownership.</done>
</task>

</tasks>

<verification>
1. Run `~/.emacs.default/bin/doom sync` after capture/agenda/denote changes.
2. Runtime check: `org-agenda` commands show journal-specific sections and include open journal TODOs from historical files.
3. Runtime check: `org-capture` journal template inserts into today's journal via helper without duplicate timestamp headings.
4. Runtime check: creating a Denote note writes to `~/org/denote/` and prompts only from the strict keyword list.
</verification>

<success_criteria>
Phase 6 outcomes are complete end-to-end: journal entries can be captured and searched, journal tasks are visible in agenda with explicit separation, and denote is active in its own strict directory/taxonomy model apart from org-roam.
</success_criteria>

<output>
After completion, create `.planning/phases/06-journaling-denote/06-02-SUMMARY.md`
</output>
