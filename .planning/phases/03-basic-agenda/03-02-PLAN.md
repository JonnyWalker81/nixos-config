---
phase: 03-basic-agenda
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - users/doom.d/config-org-agenda.el
autonomous: true
must_haves:
  truths:
    - "Daily and weekly agenda views show grouped sections ordered by priority first, then context, with Priority A actionable surfaced first after timeline."
    - "WAITING and SOMEDAY remain visible but parked at bottom sections, and uncategorized items appear in an explicit section."
    - "Weekly view shows deadlines both inside day timeline and in a dedicated weekly deadline summary section."
    - "Grouped agenda behavior persists on refresh/navigation (not temporary one-shot grouping)."
  artifacts:
    - path: "users/doom.d/config-org-agenda.el"
      provides: "Persistent org-super-agenda mode + group definitions for Phase 3 daily and weekly commands"
      contains: "after! org-super-agenda and org-super-agenda-groups"
    - path: "users/doom.d/config-org-agenda.el"
      provides: "Per-command block wiring that collects items before grouping"
      contains: "agenda/tags-todo/todo blocks for d and w"
  key_links:
    - from: "users/doom.d/config-org-agenda.el"
      to: "org-super-agenda"
      via: "global group definitions with first-match-safe ordering"
      pattern: "org-super-agenda-groups"
    - from: "users/doom.d/config-org-agenda.el"
      to: "daily/weekly command blocks"
      via: "grouped blocks attached to both d and w custom commands"
      pattern: "\(\"d\"|\(\"w\""
    - from: "users/doom.d/config-org-agenda.el"
      to: "deadline visibility"
      via: "weekly deadline summary block plus week timeline"
      pattern: "deadline|DEADLINE"
---

<objective>
Complete AGN-03 by layering robust org-super-agenda grouping onto the new daily/weekly commands and validating end-to-end behavior.

Purpose: Ensure agenda views are decision-ready by presenting urgent/contextual work in stable, meaningful sections that survive refresh and navigation.
Output: Final grouped daily/weekly agenda behavior with validation evidence for AGN-01, AGN-02, and AGN-03.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-basic-agenda/03-CONTEXT.md
@.planning/phases/03-basic-agenda/03-RESEARCH.md
@.planning/phases/03-basic-agenda/03-01-PLAN.md
@users/doom.d/config-org-agenda.el
@users/doom.d/config-org-gtd.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable persistent org-super-agenda grouping with first-match-safe ordering</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Inside `after! org-super-agenda`, enable `org-super-agenda-mode` and define persistent global groups (`setq`, not `let`) ordered for first-match correctness: timeline remains first block, then grouped actionable sections starting with Priority A actionable, followed by priority/context combinations (context as tie-breaker), explicit Uncategorized, and bottom parking sections for WAITING and SOMEDAY. Avoid broad groups before specific context groups to prevent consumption errors.</action>
  <verify>Run `rg "after! org-super-agenda|org-super-agenda-mode|org-super-agenda-groups|Priority A|WAITING|SOMEDAY|Uncategorized|:and|:order" users/doom.d/config-org-agenda.el` and confirm group definitions are persistent and ordered from specific to general.</verify>
  <done>org-super-agenda grouping is globally active, stable across refresh, and structured to honor priority-first plus context tie-break behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire grouping into both agenda commands and enforce inclusion boundaries</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Attach grouping behavior to both `d` and `w` command blocks so collection stays in agenda/tags-todo/todo queries while org-super-agenda handles section rendering. Ensure daily includes overdue + unscheduled actionable in one command, and weekly includes overdue + unscheduled grouped tasks plus dedicated weekly deadline summary block. Keep closed states hidden from planning blocks unless log mode is manually toggled.</action>
  <verify>Run `rg "\(\"d\"|\(\"w\"|org-super-agenda-groups|agenda|tags-todo|todo|DEADLINE|org-agenda-span 'week|org-agenda-span 'day|DONE|CANCELLED" users/doom.d/config-org-agenda.el` and confirm each command includes both timeline and grouped collection blocks with explicit weekly deadline summary and closed-state filtering.</verify>
  <done>Both daily and weekly commands satisfy inclusion boundaries and render grouped planning sections with the required deadline behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run validation gate for AGN-01/02/03 and capture evidence</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Run `~/.emacs.d/bin/doom sync` (or `/home/cipher/.emacs.default/bin/doom sync` if needed), restart Emacs, and validate interactively with `M-x org-agenda RET d` and `M-x org-agenda RET w`. Confirm: (1) daily shows one-day timeline plus grouped unscheduled actions with overdue included; (2) weekly starts Monday, shows full week timeline, includes grouped unscheduled tasks, and has dedicated deadline summary; (3) grouped sections show Priority A first, then context/priority sections, explicit Uncategorized, and WAITING/SOMEDAY at bottom. In each view, press `g` refresh and confirm grouping persists.</action>
  <verify>Record validation evidence in summary with: exact command keys used (`d`, `w`), proof of Monday weekly start, screenshot or textual section list showing Priority A/Uncategorized/WAITING/SOMEDAY ordering, and refresh (`g`) persistence confirmation. Include one explicit example of a deadline item appearing in both weekly timeline and deadline summary.</verify>
  <done>AGN-01, AGN-02, and AGN-03 are validated in runtime behavior with persistence across refresh and complete evidence logged in summary.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-agenda.el\") (check-parens))"` exits successfully.
2. `~/.emacs.d/bin/doom sync` (or `/home/cipher/.emacs.default/bin/doom sync`) completes.
3. Interactive agenda checks pass for both `d` and `w`, including grouping persistence after `g` refresh.
</verification>

<success_criteria>
Basic Agenda is fully functional: daily and weekly commands present timeline + grouped planning sections, weekly includes dedicated deadline summary with Monday start, and org-super-agenda grouping follows priority-first/context tie-break ordering with explicit Uncategorized and parked WAITING/SOMEDAY sections.
</success_criteria>

<output>
After completion, create `.planning/phases/03-basic-agenda/03-02-SUMMARY.md`
</output>
