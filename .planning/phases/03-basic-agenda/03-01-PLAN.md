---
phase: 03-basic-agenda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config.el
  - users/doom.d/config-org.el
  - users/doom.d/config-org-agenda.el
autonomous: true
must_haves:
  truths:
    - "User can open a dedicated daily planning agenda (`d`) that combines today's timeline with unscheduled actionable work in one command."
    - "User can open a weekly planning agenda (`w`) that spans Monday-start week view with timeline and summary blocks."
    - "Daily/weekly planning defaults avoid closed-state noise (DONE/CANCELLED hidden in planning blocks by default)."
  artifacts:
    - path: "users/doom.d/config-org-agenda.el"
      provides: "Single owner for Phase 3 agenda command definitions and agenda metadata defaults"
      contains: "org-agenda-custom-commands with keys d and w"
    - path: "users/doom.d/config.el"
      provides: "Deterministic loader order with config-org-agenda loaded after config-org-gtd"
      contains: "load! \"config-org-agenda\""
    - path: "users/doom.d/config-org.el"
      provides: "No competing org-agenda-custom-commands ownership in legacy org config"
      contains: "Agenda custom command mutation removed"
  key_links:
    - from: "users/doom.d/config.el"
      to: "users/doom.d/config-org-agenda.el"
      via: "module loader sequencing after GTD config"
      pattern: "load! \"config-org-agenda\""
    - from: "users/doom.d/config-org-agenda.el"
      to: "org-agenda"
      via: "custom command dispatcher keys d and w"
      pattern: "\(\"d\"|\(\"w\""
    - from: "users/doom.d/config-org-agenda.el"
      to: "~/org/gtd/"
      via: "agenda collection remains from existing org-agenda-files scope"
      pattern: "agenda|tags-todo|todo"
---

<objective>
Establish Phase 3 agenda command ownership and baseline daily/weekly views in a dedicated module.

Purpose: Deliver AGN-01 and AGN-02 foundations without load-order collisions or key ownership drift from existing org config.
Output: New `config-org-agenda.el`, loader wiring in `config.el`, and baseline `d`/`w` block agenda commands ready for grouping in Plan 03-02.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-basic-agenda/03-CONTEXT.md
@.planning/phases/03-basic-agenda/03-RESEARCH.md
@.planning/phases/01-gtd-foundation/01-01-SUMMARY.md
@.planning/phases/01-gtd-foundation/01-02-SUMMARY.md
@users/doom.d/config.el
@users/doom.d/config-org.el
@users/doom.d/config-org-gtd.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dedicated agenda module and move command ownership</name>
  <files>users/doom.d/config-org-agenda.el, users/doom.d/config.el, users/doom.d/config-org.el</files>
  <action>Create `users/doom.d/config-org-agenda.el` as the sole owner of `org-agenda-custom-commands` for Basic Agenda work, and load it in `users/doom.d/config.el` immediately after `config-org-gtd`. Remove the legacy `org-agenda-custom-commands` `setq` block from `users/doom.d/config-org.el` (do not alter its babel/export/non-agenda behavior). Keep command ownership single-source to prevent key collisions with existing `w` and future Phase 4 agenda extensions.</action>
  <verify>Run `rg "load! \"config-org-agenda\"|org-agenda-custom-commands|\(\"w\" \"Work Todos\"" users/doom.d/config.el users/doom.d/config-org.el users/doom.d/config-org-agenda.el` and confirm: loader exists in `config.el`, command set is defined in `config-org-agenda.el`, and legacy `Work Todos` command mutation is removed from `config-org.el`.</verify>
  <done>Agenda command ownership is centralized in `config-org-agenda.el` with deterministic load order and no duplicate command mutation in `config-org.el`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement baseline daily and weekly block agenda commands</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Inside `after! org`, define baseline `org-agenda-custom-commands` for `d` (daily) and `w` (weekly) using block agenda structure: timeline block first (`agenda`), followed by unscheduled actionable blocks (`tags-todo`/`todo`) in the same command. Set daily span to `'day` and weekly span to `'week` with `org-agenda-start-on-weekday 1` (Monday). Include overdue in both views using Org's carry-forward behavior plus explicit block filters where needed. Include weekly dedicated deadline summary block in command `w`. Keep DONE/CANCELLED excluded from planning blocks by default while preserving agenda log mode opt-in.</action>
  <verify>Run `rg "\(\"d\"|\(\"w\"|org-agenda-span 'day|org-agenda-span 'week|org-agenda-start-on-weekday 1|deadline|tags-todo|todo" users/doom.d/config-org-agenda.el` and confirm both commands contain timeline + additional planning blocks, Monday start, and explicit weekly deadline summary wiring.</verify>
  <done>Custom commands `d` and `w` exist with baseline hybrid structures matching daily/weekly scope requirements and inclusion boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add detailed metadata defaults and agenda entry keybindings</name>
  <files>users/doom.d/config-org-agenda.el</files>
  <action>Configure agenda metadata density for planning views using command-level options (for example `org-agenda-prefix-format`) so priority/category/time/effort are visible by default in both `d` and `w`. Add Doom leader entrypoints for agenda commands using `map!` (recommended `SPC o A d` and `SPC o A w`) and keep dispatcher access via `M-x org-agenda` keys `d`/`w` intact. Do not modify Phase 2 capture keybindings.</action>
  <verify>Run `rg "org-agenda-prefix-format|map!|\(:prefix \(\"o A\"|org-agenda nil \"d\"|org-agenda nil \"w\"" users/doom.d/config-org-agenda.el users/doom.d/config-org-gtd.el` and confirm metadata prefix config plus agenda leader bindings exist, with no capture keybinding regressions in `config-org-gtd.el`.</verify>
  <done>Daily and weekly agenda commands expose rich planning metadata and are accessible through consistent Doom leader entrypoints and dispatcher keys.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-agenda.el\") (check-parens))"` exits successfully.
2. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org.el\") (check-parens))"` exits successfully.
3. Static checks confirm single command ownership, loader order, and existence of `d`/`w` commands with Monday weekly start.
</verification>

<success_criteria>
Phase 3 baseline agenda architecture is in place: dedicated agenda module loaded after GTD config, no conflicting legacy agenda command owner, and working daily/weekly block commands with planning-oriented metadata defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/03-basic-agenda/03-01-SUMMARY.md`
</output>
