---
phase: 08-integration-dashboard
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config-org-integration.el
  - tests/emacs/orglife-config-tests.el
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Task-to-roam linking is bidirectional: creating a link from a GTD task also persists backlink material on the roam target."
    - "Journal-to-heading linking is bidirectional: linked task/project headings surface journal backlinks."
    - "Backlink visibility is queryable in repeatable verification (not only implied by source-side links)."
  artifacts:
    - path: "users/doom.d/config-org-integration.el"
      provides: "bidirectional backlink write/read primitives for task<->roam and journal<->heading flows"
      exports: ["org-life-link-task-to-roam", "org-life-link-journal-to-heading"]
    - path: "tests/emacs/orglife-config-tests.el"
      provides: "ERT coverage asserting target-side backlink persistence and retrieval"
      contains: "orglife-integration"
  key_links:
    - from: "users/doom.d/config-org-integration.el"
      to: "org-entry-put"
      via: "target-side backlink property write during link creation"
      pattern: "org-entry-put"
    - from: "tests/emacs/orglife-config-tests.el"
      to: "users/doom.d/config-org-integration.el"
      via: "batch tests calling integration link commands and asserting reverse-link artifacts"
      pattern: "org-life-link-task-to-roam|org-life-link-journal-to-heading"
---

<objective>
Close Phase 8 UX-01 and UX-02 verification gaps by making integration links explicitly bidirectional and test-proven.

Purpose: The current implementation writes only source-side links/metadata; this plan adds target-side backlink material so linked roam notes and GTD headings expose provenance.
Output: Backlink persistence and query helpers in the integration module plus regression tests proving reverse-link visibility.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-dashboard/08-CONTEXT.md
@.planning/phases/08-integration-dashboard/08-integration-dashboard-VERIFICATION.md
@.planning/phases/08-integration-dashboard/08-01-SUMMARY.md
@users/doom.d/config-org-integration.el
@tests/emacs/orglife-config-tests.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add target-side backlink persistence for task->roam and journal->heading flows</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Extend existing link creation primitives so each successful source->target link write also records target-side backlink data. Persist backlink artifacts on target headings/notes using Org property APIs (for example namespaced ORGLIFE backlink properties or append-safe list entries), including source ID/title/file and link kind. Ensure this write happens for both `org-life-link-task-to-roam` and `org-life-link-journal-to-heading`. Avoid fragile drawer string edits; keep all writes idempotent so repeated linking does not duplicate identical backlink records.</action>
  <verify>`rg "org-life-link-task-to-roam|org-life-link-journal-to-heading|org-entry-put|ORGLIFE_" users/doom.d/config-org-integration.el` returns target-write paths; `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-integration.el\") (check-parens))"` exits 0.</verify>
  <done>Both link commands persist source-side link metadata and target-side backlink artifacts without duplicate spam on repeated runs.</done>
</task>

<task type="auto">
  <name>Task 2: Add backlink retrieval surface used by verification and UX views</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Add a small retrieval API that reads normalized backlink records from linked targets and returns structured values suitable for tests and interactive display (e.g., list of plist/alist records with source id/title/file/kind). Wire existing interactive flows so users can inspect backlinks from linked targets (either by dedicated command or by integrating with existing integration inspection helpers). Keep this internal to integration ownership; do not modify roam core modules.</action>
  <verify>`rg "backlink|org-life-.*backlink" users/doom.d/config-org-integration.el` finds retrieval command/helpers; batch eval of helper against fixture headings returns non-empty structured records after linking.</verify>
  <done>There is a deterministic, callable path that proves linked targets expose backlink records for both GTD->roam and journal->heading links.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ERT coverage for bidirectional persistence and visibility</name>
  <files>tests/emacs/orglife-config-tests.el</files>
  <action>Add focused ERT tests that create temporary source/target headings, run task->roam and journal->heading link commands, and assert: (1) source-side id link + metadata still exist, (2) target-side backlink artifacts are written, (3) retrieval helper returns expected source records. Keep tests hermetic by using temp buffers/files and restoring point/window state. Update expected test counts in the test runner only if needed.</action>
  <verify>`tests/run-orglife-tests.sh` passes with new integration tests included.</verify>
  <done>Regression suite fails if bidirectional backlink writes or retrieval behavior regresses.</done>
</task>

</tasks>

<verification>
Run the OrgLife test suite and an explicit batch fixture script that links one GTD task to one roam note and one journal entry to one project heading, then confirms backlink records are present on both targets.
</verification>

<success_criteria>
- Task->roam linking writes reverse backlink material on the roam target.
- Journal->heading linking writes reverse backlink material on the GTD target.
- Automated tests assert backlink persistence and retrieval for both flows.
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-dashboard/08-04-SUMMARY.md`
</output>
