---
phase: 08-integration-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - users/doom.d/config-org-integration.el
autonomous: true
must_haves:
  truths:
    - "User can discover all OrgLife workflows under one `SPC o` hierarchy with mnemonic grouping."
    - "Existing muscle-memory keypaths (including `SPC o r` and `SPC o j t`) continue to work as aliases."
    - "Core workflows (capture, agenda/reviews, GTD inbox, roam, journal, denote, dashboard) are reachable from the canonical org-life map."
  artifacts:
    - path: "users/doom.d/config-org-integration.el"
      provides: "canonical SPC o map and alias preservation"
      contains: "map! :leader (:prefix (\"o\" . \"org-life\") ...)"
    - path: "users/doom.d/config-org-integration.el"
      provides: "mnemonic command labels for discoverability"
      contains: ":desc"
  key_links:
    - from: "users/doom.d/config-org-integration.el"
      to: "users/doom.d/config-org-agenda.el"
      via: "org-agenda command dispatch by command keys"
      pattern: "org-agenda nil \"[drRwHIW]\""
    - from: "users/doom.d/config-org-integration.el"
      to: "users/doom.d/config-org-roam.el"
      via: "reuse existing org-life roam commands"
      pattern: "org-life-roam-node-find|org-life-roam-node-insert"
---

<objective>
Unify OrgLife interaction under a canonical `SPC o` key tree while preserving legacy aliases and mnemonic discoverability.

Purpose: Satisfy UX-03 with a consistent navigation contract that integrates existing module commands instead of duplicating workflow ownership.
Output: One canonical key hierarchy in integration module, plus legacy-compatible aliases for previously trained keypaths.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-dashboard/08-CONTEXT.md
@.planning/phases/08-integration-dashboard/08-RESEARCH.md
@.planning/phases/04-advanced-agenda-gtd-reviews/04-01-SUMMARY.md
@.planning/phases/05-knowledge-base/05-01-SUMMARY.md
@.planning/phases/06-journaling-denote/06-01-SUMMARY.md
@users/doom.d/config-org-integration.el
@users/doom.d/config-org-agenda.el
@users/doom.d/config-org-roam.el
@users/doom.d/config-org-journal.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical org-life dispatcher under SPC o</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Add a canonical `map!` tree rooted at `SPC o` with domain-first grouping and mnemonic labels (gtd, agenda/review, journal, roam, denote, dashboard, capture actions). Include direct high-frequency shortcuts where helpful (for example capture and review entrypoints), but do not force strict two-keystroke depth when a clearer mnemonic branch is better. Reuse existing command functions; do not fork agenda/review command ownership from `config-org-agenda.el`.</action>
  <verify>`rg "\(:prefix \(\"o\" \. \"org-life\"\)\)|org-agenda nil|my/org-capture-dwim|org-life-roam" users/doom.d/config-org-integration.el` returns matches.</verify>
  <done>Canonical `SPC o` key hierarchy exists with discoverable descriptions and command coverage across all OrgLife subsystems.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve legacy keypaths as explicit aliases</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Keep legacy paths working by mapping alias entrypoints that preserve already-established keys (including `SPC o r` and `SPC o j t`) while steering users toward the canonical tree via consistent descriptions. Alias behavior must call the same underlying commands as canonical bindings so behavior stays identical. Do not remove existing module-local bindings unless replaced one-for-one in this file.</action>
  <verify>In Emacs batch eval, `(where-is-internal #'org-life-roam-node-find)` and `(where-is-internal #'org-life-journal-open-today)` include `SPC o` paths after loading config; static check with `rg "SPC o r|SPC o j t|org-life-journal-open-today|org-life-roam-node-find" users/doom.d/config-org-integration.el` returns alias definitions/descriptions.</verify>
  <done>Legacy keypaths continue to resolve and canonical `SPC o` map becomes the single discoverability surface.</done>
</task>

<task type="auto">
  <name>Task 3: Add keymap verification harness for UX-03 coverage</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Add a lightweight verification helper command or eval snippet in the module comments that checks required command reachability (`capture`, `daily review`, `weekly review`, `roam find`, `journal today`, `inbox`, `dashboard open/refresh`). Keep this test-oriented and non-invasive (no startup side effects). Use it in plan verification to prove map completeness.</action>
  <verify>Run `~/.emacs.default/bin/doom sync` then batch-evaluate the helper to confirm required commands return at least one keybinding each via `where-is-internal`.</verify>
  <done>UX-03 has reproducible command-coverage evidence tied to concrete `SPC o` entrypoints.</done>
</task>

</tasks>

<verification>
Open which-key on `SPC o` in interactive Emacs and confirm each subsystem branch is present, mnemonic, and points to working commands while legacy aliases remain functional.
</verification>

<success_criteria>
- Canonical `SPC o` tree covers all OrgLife workflows listed in UX-03.
- Legacy keypaths remain usable as aliases, including `SPC o r` and `SPC o j t`.
- Reachability checks pass for capture, reviews, roam find, journal today, and dashboard actions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-dashboard/08-02-SUMMARY.md`
</output>
