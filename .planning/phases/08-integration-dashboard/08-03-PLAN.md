---
phase: 08-integration-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - users/doom.d/config-org-integration.el
autonomous: false
must_haves:
  truths:
    - "Emacs startup opens a custom operational dashboard with today's agenda first."
    - "Dashboard includes rich inbox and deadline (14-day horizon) lists plus guidance text when sections are empty."
    - "Dashboard provides quick actions for capture, daily review, weekly review, and roam find, with deterministic open behavior and manual refresh."
  artifacts:
    - path: "users/doom.d/config-org-integration.el"
      provides: "Doom dashboard widget functions and refresh/open commands"
      contains: "+doom-dashboard-functions"
    - path: "users/doom.d/config-org-integration.el"
      provides: "startup auto-open and open-time auto-refresh behavior"
      contains: "initial-buffer-choice"
    - path: "users/doom.d/config-org-integration.el"
      provides: "manual dashboard refresh keybinding"
      contains: "+doom-dashboard-reload"
  key_links:
    - from: "users/doom.d/config-org-integration.el"
      to: "doom-dashboard"
      via: "widget registration and reload lifecycle"
      pattern: "add-to-list '+doom-dashboard-functions|+doom-dashboard-reload"
    - from: "users/doom.d/config-org-integration.el"
      to: "users/doom.d/config-org-agenda.el"
      via: "agenda/review quick action dispatch"
      pattern: "org-agenda nil \"[dRr]\""
---

<objective>
Implement a startup OrgLife dashboard on top of Doom dashboard extension points, with actionable daily visibility and fast workflow entrypoints.

Purpose: Satisfy UX-04 without introducing a parallel dashboard system, while honoring locked interaction decisions (auto-show, auto-refresh, manual refresh, guidance text).
Output: Dashboard widgets and commands in integration module for today agenda, inbox, upcoming deadlines, quick actions, and deterministic open behavior.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-dashboard/08-CONTEXT.md
@.planning/phases/08-integration-dashboard/08-RESEARCH.md
@.planning/phases/04-advanced-agenda-gtd-reviews/04-02-SUMMARY.md
@users/doom.d/config-org-integration.el
@users/doom.d/config-org-agenda.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build OrgLife dashboard widgets on Doom dashboard</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Implement dashboard widget functions registered through `+doom-dashboard-functions` (do not introduce a second dashboard package). Render blocks in this order: (1) today's agenda, (2) inbox open tasks list, (3) upcoming deadlines list using a 14-day horizon, (4) quick actions. Each data block should show rich items (not count-only) and include explicit guidance text when no items exist. Keep block rendering pure/read-only; avoid mutating agenda files while drawing.</action>
  <verify>`rg "\+doom-dashboard-functions|today|inbox|deadline|14|quick" users/doom.d/config-org-integration.el` confirms widget definitions and ordering; batch `check-parens` passes.</verify>
  <done>Dashboard displays all required operational blocks with today agenda first, rich lists, 14-day deadlines, and empty-state guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Add startup/open refresh lifecycle and context-aware quick actions</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Configure startup to open the dashboard automatically (`initial-buffer-choice` / Doom-native equivalent), refresh dashboard data when opening, and provide manual refresh under the canonical org-life keymap (for example `SPC o d r` calling `+doom-dashboard-reload`). Implement context-aware open behavior for quick actions via `display-buffer` policies: agenda/reviews may reuse current window, roam/find may prefer split/reuse heuristics, while keeping dashboard interaction predictable. Include quick actions for the locked Core 4: capture, daily review, weekly review, roam find.</action>
  <verify>`rg "initial-buffer-choice|\+doom-dashboard-reload|display-buffer|my/org-capture-dwim|org-agenda nil \"r\"|org-agenda nil \"R\"|org-life-roam-node-find" users/doom.d/config-org-integration.el` returns matches; run `~/.emacs.default/bin/doom sync` successfully.</verify>
  <done>Dashboard opens on startup, supports auto/manual refresh, and quick actions use deterministic open heuristics with all Core 4 actions present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Runtime UX verification for integrated dashboard flow</name>
  <what-built>Custom startup OrgLife dashboard with ordered blocks, refresh behavior, and quick actions wired to core workflows.</what-built>
  <how-to-verify>
    1) Restart Emacs and confirm dashboard auto-opens.
    2) Confirm first visible block is today's agenda.
    3) Confirm inbox and deadline sections show item lists (or guidance text when empty), with deadlines scoped to 14 days.
    4) Use quick actions for capture, daily review, weekly review, and roam find; verify window behavior feels deterministic (not random splitting).
    5) Trigger manual refresh (canonical dashboard refresh key) after changing a TODO/deadline and confirm dashboard reflects updated state.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or provide failed step numbers and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
Phase-level verification: confirm UX-01..UX-04 are all satisfiable end-to-end after this plan, including cross-links, unified keymap discoverability, and startup dashboard behavior.
</verification>

<success_criteria>
- Startup dashboard auto-opens with today agenda first.
- Inbox and deadlines are rich lists with empty guidance text and 14-day deadline horizon.
- Core 4 quick actions (capture/daily review/weekly review/roam find) are present and usable.
- Manual refresh updates dashboard content after underlying org data changes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-dashboard/08-03-SUMMARY.md`
</output>
