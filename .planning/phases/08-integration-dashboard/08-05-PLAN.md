---
phase: 08-integration-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["08-04"]
files_modified:
  - users/doom.d/config-org-integration.el
  - tests/emacs/orglife-config-tests.el
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Every required OrgLife workflow is reachable in at most two keystrokes via `SPC o` + single key."
    - "Coverage verification enforces key-sequence length, not just command reachability."
    - "Dashboard inbox block displays an explicit pending inbox count alongside list content."
  artifacts:
    - path: "users/doom.d/config-org-integration.el"
      provides: "flattened SPC o keymap plus strict max-depth verifier and inbox count rendering"
      contains: "org-life-verify-spc-o-coverage"
    - path: "tests/emacs/orglife-config-tests.el"
      provides: "UX-03 depth assertions and UX-04 inbox-count assertions"
      contains: "orglife"
  key_links:
    - from: "users/doom.d/config-org-integration.el"
      to: "where-is-internal"
      via: "coverage helper checks each required command and validates max key length"
      pattern: "where-is-internal"
    - from: "users/doom.d/config-org-integration.el"
      to: "+doom-dashboard-functions"
      via: "inbox widget renders numeric pending count and list entries"
      pattern: "dashboard|inbox"
---

<objective>
Close the remaining UX-03 and UX-04 verification gaps by enforcing a true two-keystroke OrgLife key contract and adding explicit pending inbox count output.

Purpose: Current map reachability and dashboard rendering are functional but do not satisfy the stricter roadmap truths validated by phase verification.
Output: Flattened `SPC o` workflow map, strengthened coverage checks, and dashboard inbox count rendering with regression tests.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-dashboard/08-CONTEXT.md
@.planning/phases/08-integration-dashboard/08-integration-dashboard-VERIFICATION.md
@.planning/phases/08-integration-dashboard/08-02-SUMMARY.md
@.planning/phases/08-integration-dashboard/08-03-SUMMARY.md
@users/doom.d/config-org-integration.el
@tests/emacs/orglife-config-tests.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten required workflows to `SPC o` plus single-key actions</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Refactor the canonical OrgLife keymap so every required workflow from UX-03 is directly reachable via `SPC o <key>` (single key after prefix). Preserve mnemonic intent and keep existing legacy aliases as additional paths where useful, but do not rely on nested prefixes for required actions. Required direct actions must include capture, daily review, weekly review, inbox, roam find, journal open-today, dashboard open, and dashboard refresh.</action>
  <verify>Batch-load Doom config and evaluate `(where-is-internal #'COMMAND)` for each required command, confirming at least one `SPC o X` path where `X` is a single key; static `rg "\(:prefix \(\"o\" \. \"org-life\"\)\)|org-life-verify-spc-o-coverage" users/doom.d/config-org-integration.el` shows flattened map + verifier.</verify>
  <done>All required workflows satisfy the two-keystroke contract while existing aliases continue to function as optional secondary paths.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce max key-sequence length in keymap verification helper</name>
  <files>users/doom.d/config-org-integration.el, tests/emacs/orglife-config-tests.el</files>
  <action>Upgrade `org-life-verify-spc-o-coverage` (or successor helper) to fail when required commands are reachable only through deeper sequences. The helper must validate both presence and max depth (`SPC o` + one key) for each required command, and return actionable diagnostics naming offending commands/paths. Add ERT assertions that guard this behavior so regressions are caught automatically.</action>
  <verify>`tests/run-orglife-tests.sh` passes and includes new assertions that fail if any required command exceeds the key-depth limit.</verify>
  <done>UX-03 verification is strict and repeatable: commands pass only when reachable within the two-keystroke budget.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit pending inbox count in dashboard inbox block</name>
  <files>users/doom.d/config-org-integration.el, tests/emacs/orglife-config-tests.el</files>
  <action>Update dashboard inbox widget rendering to print a clear pending inbox count (for example `Inbox: N pending`) before or alongside the existing rich list. Keep rich item output and empty-state guidance text intact. Add/extend tests asserting numeric count display for non-empty and empty inbox fixtures, without hardcoding unrelated formatting details.</action>
  <verify>`tests/run-orglife-tests.sh` passes with dashboard inbox count assertions; `rg "inbox|pending|count" users/doom.d/config-org-integration.el tests/emacs/orglife-config-tests.el` confirms count rendering and coverage.</verify>
  <done>Dashboard explicitly communicates pending inbox count while retaining actionable list content and guidance behavior.</done>
</task>

</tasks>

<verification>
Re-run full phase verification after execution to confirm all four Phase 8 truths are now verifiable, with no remaining UX-03 or UX-04 blockers.
</verification>

<success_criteria>
- Required workflows are bound to `SPC o <single-key>` and validated by strict depth-aware checks.
- Verification helper/tests fail if a required workflow regresses to nested-only bindings.
- Dashboard inbox section includes an explicit pending count plus existing rich-list behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-dashboard/08-05-SUMMARY.md`
</output>
