---
phase: 08-integration-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config-org-integration.el
  - users/doom.d/config.el
  - users/doom.d/config-org-gtd.el
autonomous: true
must_haves:
  truths:
    - "User can create GTD task-to-roam links that remain stable after task refiles/moves."
    - "User can create journal links to specific task/project headings (not only day headers)."
    - "Cross-links are visible inline and also queryable as stable heading metadata."
  artifacts:
    - path: "users/doom.d/config-org-integration.el"
      provides: "org-id based link helpers and metadata API"
      exports: ["org-life-link-task-to-roam", "org-life-link-journal-to-heading", "org-life-integration-capture-link-prompt"]
    - path: "users/doom.d/config.el"
      provides: "deterministic integration module loading"
      contains: "load! \"config-org-integration\""
    - path: "users/doom.d/config-org-gtd.el"
      provides: "capture templates invoking integration prompt helpers"
      contains: "%(org-life-integration-capture-link-prompt ...)"
  key_links:
    - from: "users/doom.d/config-org-gtd.el"
      to: "users/doom.d/config-org-integration.el"
      via: "capture template expansion function call"
      pattern: "org-life-integration-capture-link-prompt"
    - from: "users/doom.d/config-org-integration.el"
      to: "org-id"
      via: "ID creation/read and id: link insertion"
      pattern: "org-id-get-create|org-entry-get"
---

<objective>
Create the Phase 8 integration foundation for cross-linking between GTD, roam, and journal using stable org-id semantics.

Purpose: Deliver UX-01 and UX-02 primitives early so downstream keymap and dashboard work can reuse one canonical linking API.
Output: New integration owner module plus capture-flow hooks that create dual-representation links (inline + metadata).
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-dashboard/08-CONTEXT.md
@.planning/phases/08-integration-dashboard/08-RESEARCH.md
@users/doom.d/config-org-gtd.el
@users/doom.d/config-org-roam.el
@users/doom.d/config-org-journal.el
@users/doom.d/config.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration module with org-id link primitives and metadata schema</name>
  <files>users/doom.d/config-org-integration.el</files>
  <action>Create `config-org-integration.el` as the owner for Phase 8 integration behaviors. Add helper functions that always call `org-id-get-create` on heading targets, insert visible `[[id:...][...]]` links, and persist companion metadata properties (use one namespaced schema such as `ORGLIFE_LINK_*`). Include separate command entrypoints for GTD task->roam linking and journal->task/project heading linking, and enforce heading-level targeting for journal links (not day-file-only links). Use Org property APIs (`org-entry-put` / `org-entry-get`), not manual drawer string editing, so link data stays parse-safe.</action>
  <verify>`emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-integration.el\") (check-parens))"` exits 0 and `rg "org-id-get-create|org-entry-put|ORGLIFE_LINK_" users/doom.d/config-org-integration.el` returns matches.</verify>
  <done>`users/doom.d/config-org-integration.el` contains reusable link creation commands that produce both inline `id:` links and stable ORGLIFE metadata properties for linked targets.</done>
</task>

<task type="auto">
  <name>Task 2: Wire integration module into Doom load chain</name>
  <files>users/doom.d/config.el</files>
  <action>Add deterministic `(load! "config-org-integration")` in `config.el` after existing org subsystem modules and before purely visual polish modules so link/key/dashboard integrations initialize in one place. Preserve existing module order ownership (GTD, agenda, roam, journal, denote, visual) and avoid moving previous loaders unless required for correctness.</action>
  <verify>`rg "config-org-integration|config-org-journal|config-org-denote|config-org-visual" users/doom.d/config.el` shows integration loader added in the org module sequence.</verify>
  <done>Integration module is loaded by default in Doom bootstrap with no regression to prior org module ownership boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add capture-flow link prompts for hybrid link creation</name>
  <files>users/doom.d/config-org-gtd.el, users/doom.d/config-org-integration.el</files>
  <action>Implement capture-time helper(s) in integration module that optionally prompt for a roam node or heading target and return insertion text for templates. Update canonical capture templates in `config-org-gtd.el` to call these helper(s) using `%(...)` template expansion in relevant flows (at least task and journal templates), while keeping manual commands available for post-capture linking. Keep existing template keys/contracts intact (`t/i/p/m/j`) and avoid replacing established template ownership.</action>
  <verify>`rg "org-life-integration-capture-link-prompt|%\(org-life-" users/doom.d/config-org-gtd.el users/doom.d/config-org-integration.el` returns matches; run `~/.emacs.default/bin/doom sync` successfully.</verify>
  <done>Capture flows support prompt-driven linking while manual link commands remain available, satisfying the locked hybrid creation decision.</done>
</task>

</tasks>

<verification>
Confirm UX-01 and UX-02 foundations by creating one GTD task linked to a roam note and one journal entry linked to a task/project heading; both must produce visible `id:` links plus ORGLIFE metadata properties.
</verification>

<success_criteria>
- GTD->roam link command inserts an `id:` link and metadata on the source heading.
- Journal->task/project command targets a concrete heading ID and inserts a visible link.
- Capture templates can prompt for links without breaking existing template key contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-dashboard/08-01-SUMMARY.md`
</output>
