---
phase: 05-knowledge-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config.el
  - users/doom.d/config-org-roam.el
autonomous: true
must_haves:
  truths:
    - "org-roam is configured as a first-class module with a dedicated directory at ~/org/roam/ and autosync enabled."
    - "NixOS path/symlink behavior does not break org-roam path resolution (file-truename + visit-truename safeguards are applied)."
    - "User can find/open roam notes and insert roam links from any org buffer via stable keybound entrypoints."
  artifacts:
    - path: "users/doom.d/config-org-roam.el"
      provides: "Core org-roam configuration and command entrypoints"
      contains: "org-roam-directory, autosync setup, find/insert commands, sqlite readiness checks"
    - path: "users/doom.d/config.el"
      provides: "Deterministic module load order for org-roam config"
      contains: "load! \"config-org-roam\" after existing org modules"
  key_links:
    - from: "users/doom.d/config-org-roam.el"
      to: "~/org/roam/"
      via: "org-roam-directory uses file-truename-backed absolute path"
      pattern: "org-roam-directory|file-truename"
    - from: "users/doom.d/config-org-roam.el"
      to: "org-roam sqlite database"
      via: "db autosync mode and sqlite availability check"
      pattern: "org-roam-db-autosync|sqlite-available-p"
    - from: "users/doom.d/config-org-roam.el"
      to: "interactive note workflows"
      via: "keybindings call org-roam-node-find and org-roam-node-insert"
      pattern: "org-roam-node-find|org-roam-node-insert|map!"
---

<objective>
Establish the org-roam foundation for Phase 5 with Nix-safe path handling and day-one note navigation flows.

Purpose: Deliver KB-01, KB-02, and KB-03 prerequisites by adding a dedicated org-roam module that survives Doom load order and Nix symlink edge cases.
Output: New `config-org-roam.el` loaded by `config.el`, with org-roam core settings, autosync, and find/insert entrypoints.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-knowledge-base/05-CONTEXT.md
@users/doom.d/init.el
@users/doom.d/config.el
@users/doom.d/config-org-gtd.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dedicated org-roam module and wire load order</name>
  <files>users/doom.d/config-org-roam.el, users/doom.d/config.el</files>
  <action>Create `users/doom.d/config-org-roam.el` as the single owner for org-roam configuration. Load it from `users/doom.d/config.el` after existing org modules so command/key ownership is deterministic and future Phase 6/8 integrations have a stable base.</action>
  <verify>Run `rg "load! \"config-org-roam\"|config-org-roam|org-roam" users/doom.d/config.el users/doom.d/config-org-roam.el` and confirm module loader wiring plus new config owner file.</verify>
  <done>org-roam configuration is isolated in a dedicated module and loaded predictably.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Nix-safe org-roam directory + sqlite autosync</name>
  <files>users/doom.d/config-org-roam.el</files>
  <action>In `after! org`/`after! org-roam` safe blocks, configure `org-roam-directory` to `~/org/roam/` using `file-truename`, ensure the directory exists, and set path-handling safeguards (`find-file-visit-truename`) to avoid symlink resolution failures. Enable org-roam DB autosync and add an explicit sqlite readiness guard that surfaces actionable feedback if `(sqlite-available-p)` is nil.</action>
  <verify>Run `rg "org-roam-directory|file-truename|find-file-visit-truename|org-roam-db-autosync|sqlite-available-p|make-directory" users/doom.d/config-org-roam.el` and confirm all safeguards and autosync are present.</verify>
  <done>KB-01 core setup is complete and Nix symlink pitfalls are addressed in config.</done>
</task>

<task type="auto">
  <name>Task 3: Add baseline note find/insert entrypoints</name>
  <files>users/doom.d/config-org-roam.el</files>
  <action>Add discoverable keybindings for note search/open (`org-roam-node-find`) and link insertion (`org-roam-node-insert`) under the existing Org leader hierarchy. Ensure these commands are callable from normal org editing flow so create-or-link behavior is available while writing.</action>
  <verify>Run `rg "org-roam-node-find|org-roam-node-insert|map!|SPC o" users/doom.d/config-org-roam.el` and confirm both commands are keybound and reachable from Org workflow.</verify>
  <done>User can open existing roam notes and insert links while typing in org buffers.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-roam.el\") (check-parens))"` exits successfully.
2. `~/.emacs.d/bin/doom sync` succeeds after adding module wiring.
3. Runtime checks confirm `(sqlite-available-p)` is t and both `org-roam-node-find`/`org-roam-node-insert` work.
</verification>

<success_criteria>
Phase 5 has a stable org-roam core: dedicated module ownership, Nix-safe directory resolution, autosyncing sqlite DB, and working fuzzy find/link insertion commands.
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-base/05-01-SUMMARY.md`
</output>
