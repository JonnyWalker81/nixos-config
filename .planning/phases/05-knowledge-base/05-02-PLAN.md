---
phase: 05-knowledge-base
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - users/doom.d/config-org-roam.el
autonomous: true
must_haves:
  truths:
    - "User has three typed roam capture templates (default, literature, concept) with metadata-rich front matter and practical capture flow."
    - "Backlinks are visible from note context and ordered for recency-first review in dense graphs."
    - "Filename and title/duplicate handling follow a stable best-practice policy aligned to timestamped note identity and alias-first discoverability."
  artifacts:
    - path: "users/doom.d/config-org-roam.el"
      provides: "Typed org-roam capture templates"
      contains: "org-roam-capture-templates entries for default/literature/concept notes"
    - path: "users/doom.d/config-org-roam.el"
      provides: "Backlink display configuration"
      contains: "org-roam buffer/backlinks section behavior"
    - path: "users/doom.d/config-org-roam.el"
      provides: "Note identity policy"
      contains: "timestamp+slug filename strategy and alias-friendly defaults"
  key_links:
    - from: "users/doom.d/config-org-roam.el"
      to: "org-roam note creation flow"
      via: "template keys map to default/literature/concept note types"
      pattern: "org-roam-capture-templates|default|literature|concept"
    - from: "users/doom.d/config-org-roam.el"
      to: "current note context"
      via: "backlinks buffer/sections show inbound links"
      pattern: "backlinks|org-roam-buffer"
    - from: "users/doom.d/config-org-roam.el"
      to: "long-term note discoverability"
      via: "ROAM_ALIASES and filename policy for renamed/similar titles"
      pattern: "ROAM_ALIASES|slug|title"
---

<objective>
Implement typed capture and backlinks behavior so org-roam becomes a usable daily knowledge base, not just a note store.

Purpose: Deliver KB-04 and KB-05 while locking practical note identity decisions (rename/duplicate handling) from Phase 5 context.
Output: Expanded `config-org-roam.el` with three capture templates, backlinks configuration, and explicit filename/title policy.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-knowledge-base/05-CONTEXT.md
@.planning/phases/05-knowledge-base/05-01-SUMMARY.md
@users/doom.d/config-org-roam.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three org-roam capture templates with rich metadata</name>
  <files>users/doom.d/config-org-roam.el</files>
  <action>Define `org-roam-capture-templates` for: (1) default note (minimal fast capture), (2) literature note (source-oriented, flexible fields), and (3) concept note (definition-first). Use timestamp+slug file naming and include metadata that improves retrieval (`#+title`, `#+date`, optional `#+filetags`, aliases where relevant). Keep templates quick enough for frequent capture.</action>
  <verify>Run `rg "org-roam-capture-templates|default|literature|concept|file\+head|%<%Y|ROAM_ALIASES|filetags" users/doom.d/config-org-roam.el` and confirm all three templates plus metadata scaffolding exist.</verify>
  <done>Three typed capture templates are available and aligned with context decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Configure backlinks buffer behavior for practical navigation</name>
  <files>users/doom.d/config-org-roam.el</files>
  <action>Configure org-roam buffer/backlinks sections so current note context clearly shows inbound links with enough surrounding context for triage. Prefer a compact but informative display and tune ordering to prioritize newest backlinks first when feasible through available org-roam sorting knobs.</action>
  <verify>Run `rg "backlinks|org-roam-buffer|org-roam-mode-sections|sort" users/doom.d/config-org-roam.el` and confirm backlinks view settings and ordering intent are explicit.</verify>
  <done>User can open a note and immediately inspect what links to it in a readable backlinks view.</done>
</task>

<task type="auto">
  <name>Task 3: Lock title-change and duplicate-title policy with aliases</name>
  <files>users/doom.d/config-org-roam.el</files>
  <action>Implement and document a clear policy for title changes and similar titles, aligned with best-practice org-roam workflows: preserve stable timestamped filenames by default, avoid fragile auto-rename behavior, and rely on aliases for discoverability while allowing duplicate titles where needed. Ensure node-find/create flow remains smooth when similar names exist.</action>
  <verify>Run `rg "alias|ROAM_ALIASES|title|slug|rename|duplicate|org-roam-node-find" users/doom.d/config-org-roam.el` and confirm policy is encoded or clearly documented in config choices.</verify>
  <done>Note identity behavior is predictable over time and does not degrade search/link workflows.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-roam.el\") (check-parens))"` exits successfully.
2. `~/.emacs.d/bin/doom sync` completes.
3. Runtime validation confirms: `org-roam-capture` shows all 3 templates and `org-roam-buffer-toggle` exposes backlinks for seeded cross-linked notes.
</verification>

<success_criteria>
User has a working typed note-capture system and contextual backlinks flow, with durable filename/alias policy that keeps org-roam discoverable as note volume grows.
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-base/05-02-SUMMARY.md`
</output>
