---
phase: 05-knowledge-base
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - users/doom.d/packages.el
  - users/doom.d/config-org-roam.el
autonomous: true
must_haves:
  truths:
    - "org-roam-ui is installed/configured and can launch an interactive graph view from Emacs."
    - "Graph defaults prioritize readability and local-note neighborhood exploration over full-graph noise."
    - "All Phase 5 requirements (KB-01 through KB-06) are validated end-to-end with explicit runtime evidence."
  artifacts:
    - path: "users/doom.d/packages.el"
      provides: "org-roam-ui package declaration"
      contains: "package! org-roam-ui"
    - path: "users/doom.d/config-org-roam.el"
      provides: "org-roam-ui runtime config and launch commands"
      contains: "use-package!/after! org-roam-ui setup and keybound launch path"
    - path: ".planning/phases/05-knowledge-base/05-knowledge-base-VERIFICATION.md"
      provides: "Phase-wide acceptance evidence"
      contains: "KB-01..KB-06 verification steps and outcomes"
  key_links:
    - from: "users/doom.d/packages.el"
      to: "Doom package graph"
      via: "org-roam-ui package registration"
      pattern: "package! org-roam-ui"
    - from: "users/doom.d/config-org-roam.el"
      to: "browser graph visualization"
      via: "org-roam-ui launch commands bound in Org workflow"
      pattern: "org-roam-ui|open|mode|map!"
    - from: ".planning/phases/05-knowledge-base/05-knowledge-base-VERIFICATION.md"
      to: "Phase 5 acceptance gates"
      via: "explicit checks mapped to KB-01..KB-06"
      pattern: "KB-01|KB-02|KB-03|KB-04|KB-05|KB-06"
---

<objective>
Complete Phase 5 by adding org-roam-ui graph capabilities and running a full validation gate across the entire knowledge-base workflow.

Purpose: Deliver KB-06 and prove that core setup, linking, backlinks, typed templates, and graph navigation all work together in daily use.
Output: `org-roam-ui` integrated into Doom config plus a verification artifact that confirms KB-01..KB-06.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-knowledge-base/05-CONTEXT.md
@.planning/phases/05-knowledge-base/05-01-SUMMARY.md
@.planning/phases/05-knowledge-base/05-02-SUMMARY.md
@users/doom.d/packages.el
@users/doom.d/config-org-roam.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add org-roam-ui package and Emacs integration</name>
  <files>users/doom.d/packages.el, users/doom.d/config-org-roam.el</files>
  <action>Declare `org-roam-ui` in `packages.el`, then configure it in `config-org-roam.el` with sane defaults (auto-open/follow/sync options chosen for an always-available companion graph). Add launch keybinding(s) under the Org namespace so graph view is one action away from note editing.</action>
  <verify>Run `rg "org-roam-ui|package! org-roam-ui|map!|org-roam-ui-open|org-roam-ui-mode" users/doom.d/packages.el users/doom.d/config-org-roam.el` and confirm package declaration plus runtime launch path.</verify>
  <done>Graph feature is installed and directly accessible from Emacs.</done>
</task>

<task type="auto">
  <name>Task 2: Tune graph defaults for local readability and navigation</name>
  <files>users/doom.d/config-org-roam.el</files>
  <action>Set org-roam-ui options to prioritize readable labels and local-neighborhood exploration from the current node (instead of defaulting to an overwhelming global graph). Keep behavior balanced for both exploration and direct navigation.</action>
  <verify>Run `rg "org-roam-ui-.*\(open-on-start\|follow\|sync\|update\|theme\|node\|local\)" users/doom.d/config-org-roam.el` and confirm explicit graph-behavior settings are present.</verify>
  <done>Graph starts in a usable, workflow-friendly mode rather than a noisy novelty view.</done>
</task>

<task type="auto">
  <name>Task 3: Execute full Phase 5 validation gate and record evidence</name>
  <files>users/doom.d/config-org-roam.el, .planning/phases/05-knowledge-base/05-knowledge-base-VERIFICATION.md</files>
  <action>Run `~/.emacs.d/bin/doom sync`, then validate all KB requirements in live Emacs: sqlite availability, autosync behavior, node find/open, node insert create/link flow, backlinks visibility, 3 capture templates, and org-roam-ui graph launch/navigation. Capture exact command keys and observed outcomes in `05-knowledge-base-VERIFICATION.md` mapped 1:1 to KB-01..KB-06.</action>
  <verify>Verification doc contains checklists/evidence for each KB requirement and notes any caveats with reproduction steps.</verify>
  <done>Phase 5 is acceptance-verified with reproducible evidence and no unresolved requirement gaps.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-roam.el\") (check-parens))"` exits successfully.
2. `~/.emacs.d/bin/doom sync` completes with `org-roam-ui` installed.
3. Runtime validation evidence exists for KB-01 through KB-06 in `.planning/phases/05-knowledge-base/05-knowledge-base-VERIFICATION.md`.
</verification>

<success_criteria>
Phase 5 is complete when the user can capture/find/link roam notes, inspect backlinks, and launch/use an interactive graph from Emacs, with all KB requirements explicitly verified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-base/05-03-SUMMARY.md`
</output>
