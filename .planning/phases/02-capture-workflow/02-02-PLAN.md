---
phase: 02-capture-workflow
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - users/doom.d/config-org-gtd.el
autonomous: true
must_haves:
  truths:
    - "User can capture a new project with name + deadline and get a structured project skeleton in projects.org."
    - "User can capture meeting notes with date, optional attendees, notes section, and TODO action-item sub-items."
    - "Meeting action-item TODOs are captured into an agenda-scoped file under ~/org/gtd/."
  artifacts:
    - path: "users/doom.d/config-org-gtd.el"
      provides: "CAP-03/CAP-04 project and meeting templates in the same canonical org-capture template set"
      contains: "projects.org and meetings.org template targets"
    - path: "~/org/gtd/projects.org"
      provides: "Top-level destination for new project entries"
      contains: "Structured project headings from capture template"
    - path: "~/org/gtd/meetings.org"
      provides: "Dedicated meeting note destination for agenda-visible action items"
      contains: "Meeting heading with Notes and Action Items sections"
  key_links:
    - from: "users/doom.d/config-org-gtd.el"
      to: "~/org/gtd/projects.org"
      via: "project template target + prompt expansion"
      pattern: "file .*projects\.org"
    - from: "users/doom.d/config-org-gtd.el"
      to: "~/org/gtd/meetings.org"
      via: "meeting template target + TODO sub-items"
      pattern: "file .*meetings\.org"
    - from: "users/doom.d/config-org-gtd.el"
      to: "org-agenda-files"
      via: "meetings file placed under ~/org/gtd/ directory scope"
      pattern: "org-agenda-files"
---

<objective>
Complete structured capture for projects and meetings with minimal-friction prompts and agenda-compatible destinations.

Purpose: Deliver CAP-03 and CAP-04 while preserving the single-source capture architecture and Phase 1 GTD directory boundaries.
Output: Project + meeting capture templates and a validation gate that confirms final template set is active (not Doom defaults).
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-workflow/02-CONTEXT.md
@.planning/phases/02-capture-workflow/02-RESEARCH.md
@.planning/phases/02-capture-workflow/02-01-PLAN.md
@users/doom.d/config-org-gtd.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured project capture template</name>
  <files>users/doom.d/config-org-gtd.el</files>
  <action>Add a project template key targeting `~/org/gtd/projects.org` as top-level entries (not nested under an inbox heading). Capture project name and deadline at entry time, then generate a heading skeleton with sections (Outcome, Notes, Next Actions). Do not insert starter actions or pre-filled TODO children.</action>
  <verify>Run `rg "projects\.org|DEADLINE:|Outcome|Next Actions|Project" users/doom.d/config-org-gtd.el` and confirm project template includes prompts and section structure.</verify>
  <done>CAP-03 template captures project name + deadline and inserts a structured top-level project heading in `projects.org` with no starter actions.</done>
</task>

<task type="auto">
  <name>Task 2: Add meeting capture template and finalize template menu validation</name>
  <files>users/doom.d/config-org-gtd.el</files>
  <action>Add a meeting template key targeting `~/org/gtd/meetings.org` with heading + subsections for Notes and Action Items. Prompt for meeting title/date, include optional attendees capture, and create TODO sub-items for action tracking (with optional inbox linkage field in template body). Ensure this template is in the same canonical `org-capture-templates` set from 02-01. Validate final key set covers task, idea, project, and meeting templates and is designed to replace Doom defaults.</action>
  <verify>Run `rg "meetings\.org|ATTENDEES|Action Items|\*\* Notes|\*\* Action Items|TODO" users/doom.d/config-org-gtd.el` and confirm meeting template structure exists. Then run `rg "org-capture-templates" users/doom.d/config-org-gtd.el` and verify one canonical set defines 4+ templates.</verify>
  <done>CAP-04 template exists with optional attendees and TODO action items in `meetings.org`, and final menu definition includes at least task, idea, project, and meeting templates in the canonical set.</done>
</task>

<task type="auto">
  <name>Task 3: Execute end-to-end capture validation gate</name>
  <files>users/doom.d/config-org-gtd.el</files>
  <action>Perform runtime validation after `doom sync`: open Emacs, run `M-x org-capture`, and confirm menu labels/keys match Phase 2 templates (not Doom defaults). Execute one sample capture for each template (task, idea, project, meeting) and confirm entries land in the intended files/sections with expected prompt behavior.</action>
  <verify>Record validation evidence in plan summary: template menu screenshot or explicit key list, plus resulting headings in `~/org/gtd/inbox.org`, `~/org/gtd/projects.org`, and `~/org/gtd/meetings.org`.</verify>
  <done>All 4 Phase 2 success criteria are demonstrably satisfied in runtime behavior, not just static config.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-gtd.el\") (check-parens))"` exits successfully.
2. `doom sync` completes without capture-related errors.
3. Runtime gate confirms capture menu and destinations match CAP-01..CAP-04.
</verification>

<success_criteria>
Phase 2 capture workflow is complete: four capture types exist, quick captures are low-friction, project and meeting structures are generated correctly, and menu/runtime behavior reflects custom templates instead of Doom defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/02-capture-workflow/02-02-SUMMARY.md`
</output>
