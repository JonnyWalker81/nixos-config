---
phase: 02-capture-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config-org-gtd.el
  - users/doom.d/config-org.el
autonomous: true
must_haves:
  truths:
    - "User can start capture from anywhere with an explicit global hotkey for DWIM capture and a separate hotkey for full template menu."
    - "User can capture a quick inbox task with one template key and must provide task title plus exactly one GTD context tag."
    - "User can capture a timestamped idea into inbox.org with minimal prompt friction."
    - "Quick capture mostly auto-saves and, after finalize, returns to prior context by default."
  artifacts:
    - path: "users/doom.d/config-org-gtd.el"
      provides: "Single after! org setq source of truth for CAP-01/CAP-02 templates and capture entry commands"
      contains: "org-capture-templates"
    - path: "users/doom.d/config-org.el"
      provides: "No conflicting add-to-list mutation for org-capture-templates"
      contains: "Capture template mutation removed"
  key_links:
    - from: "users/doom.d/config-org-gtd.el"
      to: "org-capture"
      via: "my/org-capture-dwim using (org-capture nil KEY)"
      pattern: "org-capture nil"
    - from: "users/doom.d/config-org-gtd.el"
      to: "global-map"
      via: "explicit global-set-key bindings for DWIM and full-menu entry"
      pattern: "global-set-key"
    - from: "users/doom.d/config-org-gtd.el"
      to: "~/org/gtd/inbox.org"
      via: "file+headline targets for Tasks and Ideas"
      pattern: "file\+headline.*inbox\.org"
---

<objective>
Establish fast, reliable capture entry for inbox tasks and ideas without Doom override regressions.

Purpose: Deliver CAP-01 and CAP-02 by making quick capture low-friction, mnemonic, and stable across restarts.
Output: Updated GTD capture configuration with DWIM + full-menu entrypoints and canonical templates for task and idea capture.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-workflow/02-CONTEXT.md
@.planning/phases/02-capture-workflow/02-RESEARCH.md
@.planning/phases/01-gtd-foundation/01-01-SUMMARY.md
@users/doom.d/config-org-gtd.el
@users/doom.d/config-org.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical mnemonic template keys and CAP-01 global hotkeys</name>
  <files>users/doom.d/config-org-gtd.el</files>
  <action>Inside one existing `after! org` block, keep `org-capture-templates` as a single `setq` (do not use scattered `add-to-list`) and enforce the locked mnemonic key contract for the full Phase 2 menu: `t` = task, `i` = idea, `p` = project, `m` = meeting (no alternate keys for these four template types). Ensure CAP-01/CAP-02 entries include: (1) task template `t` targeting inbox Tasks requiring title + exactly one GTD context tag, (2) idea template `i` targeting inbox Ideas with timestamped entry. Define concrete DWIM behavior by splitting decision and execution: create `my/org-capture-dwim-key` that returns `m` when current buffer file is `~/org/gtd/meetings.org`, returns `p` when current buffer file is `~/org/gtd/projects.org`, and returns `t` otherwise; `my/org-capture-dwim` must call `(org-capture nil (my/org-capture-dwim-key))`. Wire explicit global hotkeys: bind `C-c c` to DWIM capture and bind `C-c C` to full-menu capture (`org-capture`) so CAP-01 is explicit and not implied.</action>
  <verify>Run `rg "\(\"t\" .*Task|\(\"i\" .*Idea|\(\"p\" .*Project|\(\"m\" .*Meeting|defun my/org-capture-dwim-key|defun my/org-capture-dwim|org-capture nil \(my/org-capture-dwim-key\)|global-set-key.*C-c c|global-set-key.*C-c C|file\+headline .*inbox\.org|@home|@work|@errands|@phone|@computer|@email" users/doom.d/config-org-gtd.el` and confirm the exact key contract (`t/i/p/m`) plus explicit hotkeys are present. Then run `doom sync` and runtime-verify at least two DWIM contexts: from `~/org/gtd/meetings.org`, `M-x my/org-capture-dwim` defaults to template `m`; from a non-project/non-meeting buffer (for example `*scratch*`), `M-x my/org-capture-dwim` defaults to template `t`.</verify>
  <done>`users/doom.d/config-org-gtd.el` defines one canonical template set with enforceable mnemonic keys (`t` task, `i` idea, `p` project, `m` meeting), explicit CAP-01 global hotkey wiring, and a concrete DWIM selector verified in runtime for multiple contexts.</done>
</task>

<task type="auto">
  <name>Task 2: Remove conflicting legacy capture template mutation</name>
  <files>users/doom.d/config-org.el</files>
  <action>Delete the legacy `add-to-list 'org-capture-templates` snippet from `config-org.el` so Phase 2 templates are controlled only from `config-org-gtd.el`. Do not change unrelated org-babel, export, agenda command, or keybinding sections.</action>
  <verify>Run `rg "add-to-list 'org-capture-templates|Work Todo" users/doom.d/config-org.el` and confirm no matches remain.</verify>
  <done>No capture-template mutation remains in `config-org.el`, eliminating template drift/duplication risk from Doom load order.</done>
</task>

<task type="auto">
  <name>Task 3: Implement and verify low-interruption quick capture finalize behavior</name>
  <files>users/doom.d/config-org-gtd.el</files>
  <action>Configure quick-capture behavior so it mostly auto-saves with minimal interruption: apply `:immediate-finish t` only to the quick idea template (do not apply to templates that require richer input like project/meeting). Preserve default Org finalize behavior so capture returns to prior context automatically; do not add custom window-restore/advice code around `org-capture-finalize`.</action>
  <verify>Run `rg ":immediate-finish t|Idea|org-capture-finalize|advice-add" users/doom.d/config-org-gtd.el` and confirm the quick template has `:immediate-finish t` while no custom finalize/advice override is introduced. Then run `doom sync` and manually verify: trigger quick idea capture, finalize, and confirm focus returns to the previous buffer/window context.</verify>
  <done>Quick capture is mostly auto-save for low-friction templates, and post-save flow returns immediately to prior context by default.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-gtd.el\") (check-parens))"` exits successfully.
2. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org.el\") (check-parens))"` exits successfully.
3. Static `rg` checks in task verification pass, including explicit hotkey wiring and `:immediate-finish` quick-capture behavior.
4. Runtime check confirms quick idea capture finalizes and returns to prior context.
</verification>

<success_criteria>
CAP-01 and CAP-02 wiring exists in code: explicit global hotkeys for DWIM/menu entry, task+idea templates with inbox section targets, quick auto-save behavior for low-friction capture, return-to-context default after finalize, and no conflicting template mutation in other org config files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-capture-workflow/02-01-SUMMARY.md`
</output>
