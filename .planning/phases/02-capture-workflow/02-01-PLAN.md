---
phase: 02-capture-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - users/doom.d/config-org-gtd.el
  - users/doom.d/config-org.el
autonomous: true
must_haves:
  truths:
    - "User can start capture from anywhere with a global DWIM entrypoint and still access the full capture menu."
    - "User can capture a quick inbox task with one template key and must provide task title plus exactly one GTD context tag."
    - "User can capture a timestamped idea into inbox.org with minimal prompt friction."
  artifacts:
    - path: "users/doom.d/config-org-gtd.el"
      provides: "Single after! org setq source of truth for CAP-01/CAP-02 templates and capture entry commands"
      contains: "org-capture-templates"
    - path: "users/doom.d/config-org.el"
      provides: "No conflicting add-to-list mutation for org-capture-templates"
      contains: "Capture template mutation removed"
  key_links:
    - from: "users/doom.d/config-org-gtd.el"
      to: "org-capture"
      via: "my/org-capture-dwim using (org-capture nil KEY)"
      pattern: "org-capture nil"
    - from: "users/doom.d/config-org-gtd.el"
      to: "~/org/gtd/inbox.org"
      via: "file+headline targets for Tasks and Ideas"
      pattern: "file\+headline.*inbox\.org"
---

<objective>
Establish fast, reliable capture entry for inbox tasks and ideas without Doom override regressions.

Purpose: Deliver CAP-01 and CAP-02 by making quick capture low-friction, mnemonic, and stable across restarts.
Output: Updated GTD capture configuration with DWIM + full-menu entrypoints and canonical templates for task and idea capture.
</objective>

<execution_context>
@/home/cipher/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cipher/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-workflow/02-CONTEXT.md
@.planning/phases/02-capture-workflow/02-RESEARCH.md
@.planning/phases/01-gtd-foundation/01-01-SUMMARY.md
@users/doom.d/config-org-gtd.el
@users/doom.d/config-org.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical inbox capture templates and entrypoints</name>
  <files>users/doom.d/config-org-gtd.el</files>
  <action>Inside one existing `after! org` block, define `org-capture-templates` with a single `setq` (do not use scattered `add-to-list`) and add CAP-01/CAP-02 entries: task template key for inbox Tasks section requiring title + one context tag, and idea template key for inbox Ideas section using timestamped entry with minimal prompts. Add a DWIM command that routes to a context-aware default template via `(org-capture nil KEY)` and keep a full-menu path available on a separate global keybinding.</action>
  <verify>Run `rg "org-capture-templates|org-capture nil|file\+headline .*inbox\.org|@home|@work|@errands|@phone|@computer|@email" users/doom.d/config-org-gtd.el` and confirm both inbox templates and DWIM routing are present.</verify>
  <done>`users/doom.d/config-org-gtd.el` contains one canonical capture template set with task+idea templates, global DWIM entry, and full-menu fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Remove conflicting legacy capture template mutation</name>
  <files>users/doom.d/config-org.el</files>
  <action>Delete the legacy `add-to-list 'org-capture-templates` snippet from `config-org.el` so Phase 2 templates are controlled only from `config-org-gtd.el`. Do not change unrelated org-babel, export, agenda command, or keybinding sections.</action>
  <verify>Run `rg "add-to-list 'org-capture-templates|Work Todo" users/doom.d/config-org.el` and confirm no matches remain.</verify>
  <done>No capture-template mutation remains in `config-org.el`, eliminating template drift/duplication risk from Doom load order.</done>
</task>

</tasks>

<verification>
1. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org-gtd.el\") (check-parens))"` exits successfully.
2. `emacs --batch -Q --eval "(with-temp-buffer (insert-file-contents \"users/doom.d/config-org.el\") (check-parens))"` exits successfully.
3. Static grep checks in task verification pass.
</verification>

<success_criteria>
CAP-01 and CAP-02 wiring exists in code: from-anywhere entrypoint, task+idea templates, inbox section targets, and no conflicting template mutation in other org config files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-capture-workflow/02-01-SUMMARY.md`
</output>
